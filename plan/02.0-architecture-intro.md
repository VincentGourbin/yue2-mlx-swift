## 2. Rappel d'architecture (ce que l'exécutant doit connaître — ne pas re-dériver)

Source de vérité : `src/yue2/modeling_yue2.py` (LM), `nar.py` (flow matching), `sampling.py`, `protocol.py`, `modeling_vae.py`, `tokenization_yue2.py`, `pipeline.py`. Le clone de référence vit dans le scratchpad de session ; re-cloner avec `git clone --depth 1 https://github.com/multimodal-art-projection/YuE.git`.
