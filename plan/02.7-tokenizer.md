### 2.7 Tokenizer (`tokenization_yue2.py`)

BPE tiktoken **Qwen1** (`qwen.tiktoken`, 151 643 rangs), regex Qwen, `encode_ordinary` (aucun token spécial n'est analysé dans le texte) après normalisation **NFC**. Tokens spéciaux ajoutés par le code du protocole en ids bruts (§2.1) ; `decode` ignore les ids ≥ vocab.

**Décision (vérifiée le 2026-09-16 sur 1 047 tokens : lyrics chinois/anglais, 3 fichiers ABC, instruction)** : le `tokenizer.json` de `Qwen/Qwen2.5-0.5B` (Apache-2.0) a **exactement** le même vocabulaire (0 écart sur 151 643 entrées), la même regex de pré-tokenisation et un normaliseur NFC ⇒ encodage identique. Seule différence : il analyse les chaînes `<|im_start|>`, `<|endoftext|>`… comme tokens spéciaux. ⇒ `Scripts/convert-tokenizer.py` télécharge ce fichier, **vide `added_tokens`** (et `post_processor`), vérifie l'égalité avec tiktoken sur `parity/tokenizer_corpus.json`, et écrit `$YUE2_MODELS_DIR/YuE2-3B/tokenizer.json`. Côté Swift : `swift-transformers` `AutoTokenizer.from(modelFolder:)` (pattern `flux-2 TekkenTokenizer.swift` L223-240), `encode` sans tokens spéciaux, NFC via `precomposedStringWithCanonicalMapping` avant l'appel (ceinture et bretelles).
