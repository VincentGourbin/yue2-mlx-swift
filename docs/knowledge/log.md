# Journal de la base de connaissances

- 2026-09-16 — plan rév. 1, squelette créé : paquet `YuE2Swift` (YuE2Core, yue2, yue2-bench-ui),
  scripts de validation, CLAUDE.md. Aucun fait d'architecture mesuré à ce jour.
- 2026-09-16 — référence Python : upstream ef1936f, torch 2.10.0, transformers 4.57.6, pytest upstream vert (172 passed, 11 skipped — réseau/Hub non ignorés, tout passe hors-ligne ; 59 passed sur le périmètre retenu par PLAN.md §4.2 : test_model + test_nar + test_vae + test_protocol).
- 2026-09-17 — T-1.11, GATE G-1 : `yue2 decode` bout en bout (latent `.npy` → WAV) sur `latent_40` (40 frames, VAE réel) → 76 736 échantillons, 1,6 s d'audio à 48 kHz, en 0,06 s (peak 1767 MB). Pas de latent long disponible avant T-3.6 (`real_fixtures.py song`).
- 2026-09-18 — T-2.10, `yue2 plan`/`yue2 semantic` bout en bout sur `reference/yue/examples/song.json` (bf16, poids réels) : phase ABC 572 tokens à 59,6 tok/s (score.abc bien formé : X:/M:/L:/K: dans l'en-tête) ; phase sémantique 300 tokens à 66,9 tok/s (`--semantic-max-tokens 300`, tronqué). Les deux au-dessus du seuil ≥ 30 tok/s de la fiche.
- 2026-09-18 — T-3.6, GATE G-4 : chanson complète bout en bout (`reference/yue/examples/song.json`, seed 831001, réglages par défaut) : 63,0 s d'audio générées en 89,1 s (abc 9,1 s, sémantique 23,8 s, nar 54,0 s, vae 2,2 s), aucune troncature. Écoutée par Vincent : « une belle musique », GATE validée.
- 2026-09-18 — T-4.0, GATE G-5 : référence de perf froide (`Scripts/bench-song.sh`, 120 s de refroidissement, 2 runs A/A, 0 % dispersion) : chanson courte (64/100/4) abc=1,0s sem=1,3s nar=0,6s vae=0,1s peak=9969 MB. Sur la chanson complète profilée, le NAR domine à ≈ 67 % du temps total (RTF 0,82). Écart chaud/froid observé et documenté : NAR/VAE 2-4× plus lents à chaud (juste après plusieurs builds) qu'après repos GPU — toute mesure de perf doit refroidir d'abord.
- 2026-09-18 — T-4.1, O4 : `Memory.cacheLimit` par étape (AR 2 Go / NAR 4 Go / VAE 1 Go) est actif sans interruption depuis T-1.11 ; nouvelle mesure froide (`bench-song.sh`) : abc=1,0s sem=1,4s nar=0,6s vae=0,1s peak=9971 MB, 0 % dispersion — stable avec la référence T-4.0, confirmant qu'aucune régression n'a été introduite depuis. Isoler le gain exact demanderait de retirer temporairement une garantie mémoire déjà en production ; non tenté (cf. journal).
- 2026-09-18 — T-4.2, O2 : réutilisation du cache KV sémantique pour le préfixe NAR (chunk unique, `guidance == 1`) — chanson courte (`bench-song.sh`) : nar 0,6s → 0,5s (−17 %, satisfait le critère de la fiche) ; chanson complète : nar 51,5s → 50,9s (−1,2 % seulement — l'estimation du plan « 10-20 s » était optimiste pour un préfixe de cette taille, ≈2 277 tokens, dont le préfill neuf ne coûtait réellement qu'≈0,8-1 s au débit mesuré ailleurs). Gardée : optimisation strictement soustractive et prouvée bit-exacte (`KVCacheReuseTests`), jamais plus lente.
- 2026-09-18 — T-4.3, O8 : buffers K/V contigus par couche pour le NAR (évite un `concatenated` par couche à chaque `velocity()`) — implémenté puis **retiré** : gain nul sur la chanson courte (nar 0,5s→0,5s), ≈1,6 % seulement sur la chanson complète (nar 50,9s→50,1s), sous le seuil de 5 % sur les deux instruments. Piège MLX découvert au passage (corrigé avant la mesure, puis le code entier retiré) : muter en place (`buffer[...] = k`) un `MLXArray` issu d'un `concatenated` **jamais évalué** peut aliaser le stockage source sous pression mémoire — `eval()` obligatoire avant tout subscript-set.
- 2026-09-18 — T-4.4, O7 : `compile(shapeless: true)` de `SnakeBeta.activation` (coupe-circuit `YUE2_DISABLE_COMPILE=1`) — gain nul sur la chanson courte (déjà au plancher de résolution), **≈30 %** sur le décodage VAE de la chanson complète (2,0s → 1,4s). Gardée. Parité identique avec/sans le kill-switch.
- 2026-09-18 — T-4.5, O5 : quantification 8-bit affine (groupe 64) des projections `self_attn.*_proj`/`mlp.*` de la voie AR uniquement (`--quant qint8`, opt-in, `--quant-head` en option) — chanson courte (A/B/B/A, `bench-song.sh`) : phase AR (abc+sem) 2,3s → 1,6-1,8s (≈26-30 %) ; chanson complète : AR 31,5s → 22,0s (−30,2 %), débit sémantique 68,7 → 96,6 fr/s (+40,6 %), NAR/VAE strictement inchangés (confirmant l'exclusion `nar_*`), pic mémoire process −8,9 %. Parité qint8 vs bf16 (mêmes préfixes réels que `RealLMParityTests`) : rel=0,0051 (ABC)/0,0045 (sémantique), 10× sous le seuil de la fiche (5e-2) ; greedy 8/8 sur les deux préfixes, y compris avec `--quant-head` (dé-quantification des lignes du `RestrictedHead`, jamais `asType(weight.dtype)` sur un `QuantizedLinear`, piège n°10). Gardée, strictement opt-in (défaut `.none`/bf16 inchangé). Écoute humaine non faite par l'agent (pas de capacité audio dans cet environnement) — demandée à Vincent dans `tasks/ASK.md`. Piège d'infrastructure découvert au passage : bash 3.2 (macOS) traite `"${arr[@]}"` comme non défini sous `set -u` même après `arr=()`, et `${arr[@]-}` seul ne le corrige pas (il injecte un argument vide) — l'idiome sûr est `"${arr[@]+"${arr[@]}"}"` (corrigé dans `Scripts/check-tests.sh` et `Scripts/bench-song.sh`, qui accepte maintenant des arguments passthrough pour les futures fiches A/B).
- 2026-09-18 — T-4.6, O6/O9 : les deux garde-fous de la fiche se sont révélés négatifs, aucun code changé. O6 (tuilage des requêtes NAR) : la mémoire échantillonnée dans trois traces `Flow Matching` (commits différents) ne montre qu'un delta de 100-349 Mo pendant la phase, incompatible avec la matérialisation persistante d'une allocation `[16, L_nar, L_ar+L_nar]` (≈194 Mo/couche pour ce préfixe) que `queryChunkSize` viserait à réduire — noyau `MLXFast.scaledDotProductAttention` déjà fusionné. O9 (CFG batché en mode off) : le mode CFG à deux branches ne s'active que pour `cot == off` (`SongRequest.guidance` = 1.01 uniquement dans ce cas) ; tout le port est validé et mesuré avec `cot: full` (`guidance == 1`, une seule branche, rien à batcher) — `cot: off` reste supporté mais n'apparaît dans aucun banc de mesure ni démo validée à ce jour.
- 2026-09-20 — T-6.3, E3, Core AI SDK contracts (unblocks the prior "no docs" finding): the real `.swiftinterface` files ship inside the SDK itself (`MacOSX27.0.sdk/System/Library/SubFrameworks/{CoreAIRuntime,CoreAIDelegates,CoreAIAsset}.framework/.../*.swiftinterface`) — the earlier session's "no documentation at all" was a search miss (it only inspected `CoreAI.framework`'s own umbrella `.tbd`, not the subframeworks it re-exports), not a genuine absence. Confirmed API: `AIModel.init(contentsOf:options:) async throws`, `loadFunction(named:) throws -> InferenceFunction?`, `InferenceFunction.run(inputs:states:outputViews:) async throws -> Outputs` (outputs always row-major contiguous), `NDArray.init(scalars:shape:)` (simplest write path, copies) and `NDArray.View.withUnsafePointer` (simplest read path). **Compute-unit safety, definitively answered**: `SpecializationOptions` only offers `.default`, `.cpuOnly`, and `init(preferredComputeUnitKind:)` — there is no initializer/setter to restrict `allowedComputeUnitKinds` to a subset that *excludes* CPU, and no public API to inspect which unit actually ran a given op after specialization. A silent CPU fallback for an incompatible op pattern cannot be prevented or detected at the API level in this beta SDK — a real framework limitation. Empirically observed evidence of an *ANE* equivalent of this risk: specializing the VAE decoder (Oobleck, transposed convs) for `.neuralEngine` prints `ANE regionCall op not found` / `Broadcast can only be done up to the max tensor size (16384) in the dimension W` for several `Conv1d`/`ResidualUnit` ops during specialization (cache miss only, silent on cache hit) — i.e., those ops provably do not run on ANE despite the preference, with zero way to detect this at runtime through public API, only by reading stderr during the one-time specialization. Measured SNR vs MLX fp32 (fp16 decoder, `Scripts/coreai/export_vae.py`'s 3 enumerated shapes): exact-shape (288 frames, no tiling/padding) GPU = 55.3 dB (matches Python's own `check_asset.py`, 54.9-55.8 dB — the Swift bridge itself is correct), ANE = 2.0 dB (unusable, consistent with the fallback warnings above); tiled 1100-frame (production-shaped, `decodeTiled`'s 1024/16 tile/halo, needs zero-padding up to the nearest enumerated shape since Core AI requires an exact shape) GPU = 33.75 dB (below this ticket's 40 dB gate — a real, unexplained-so-far degradation specific to the pad-to-enumerated-shape technique, since the identical crop-with-halo algorithm gets rel≈2e-6 in pure MLX), ANE tiled = 16.9 dB (compounds both effects). Ticket left `bloquée` pending Vincent's decision (`tasks/ASK.md`); code (`NDArrayBridge.swift`, `CoreAIVAEDecoder.swift`, `VAEDecoding.swift`, `VAEBackendLoader.swift`) builds and is committed, `CoreAIVAETests.swift` was written but not committed (fails today, house rule: never commit a red test).
- 2026-09-20 — T-6.4, E4, NAR stack in Core AI: two durable findings beyond the fiche's own outcome (`tasks/JOURNAL.md` has the full numbers). **(1) Lifetime-dependent-value API contract, confirmed a second time**: `NDArray.view(as:)` returns a value whose borrow must be traceable to a named local `let` spanning the whole call site — calling it directly on an unnamed temporary (`NDArrayBridge.ndArray(from: x).view(...)`) or on a bare stored-property read (`self.someND.view(...)`) only gets the compiler a borrow scoped to that one statement, too short to cover a later `InferenceFunction.run(inputs:)` in the same method (`error: lifetime-dependent value escapes its scope`). `CoreAIVAEDecoder.decode` (T-6.3) already used the safe pattern (bind to `let` first) without this being written down as a rule; now it is — any future Core AI bridge code should bind every `NDArray` to a local before `.view(as:)`. **(2) ANE + int8 `coreai.dequantize` is a hard crash, not just imprecise**: `SpecializationOptions(preferredComputeUnitKind: .neuralEngine)` on an asset using `Int8Linear` (weight-only int8 via `torch.ops.coreai.dequantize`) doesn't silently fall back or degrade — it prints `ANE regionCall op not found` during specialization (same warning family as T-6.3's VAE ANE conv-transpose finding) and then aborts the process (`SIGABRT`, exit 134) the first time the function actually runs, with `ANE inference operation failed due to unknown error`. This is `coreai-torch` 0.4.2 on this SDK; the practical rule is: never ship an ANE preference for an asset with int8-dequantize ops without a parity/smoke run confirming the process doesn't abort, not just checking the numeric output. **(3) Core AI GPU is markedly slower than MLX for this workload**: one-shot `bench-coreai-nar` timing (no cooldown/A-B-B-A rigor, informational only per this command's own docstring) showed Core AI GPU ≈7-8× slower than MLX per NAR `velocity` evaluation (205ms→1586ms at L=256, 773ms→7001ms at L=1024), plus a multi-second one-time K/V-to-`NDArray` conversion cost per chunk. Unlike the VAE (T-6.3, where the blocker was precision), here Core AI GPU is numerically correct (`PARITY OK`) but likely not worth shipping purely on throughput grounds — deferred to G-9 (iPhone measurement) per the fiche's own closing criterion, since ANE (the unit this would matter most for on a phone) isn't usable at all yet.
- 2026-09-20 — T-6.4b, E4 corrections, closing the NAR Core AI question: **(1) the `NDArray`/`MLXArray` bridge's real minimal-copy pattern, confirmed working**: `MLXArray.asData(access: .noCopyIfContiguous)` (zero-copy on the MLX side right after `eval()` of a fresh `asType` cast, always contiguous) memcpy'd directly into `NDArray(shape:scalarType:).mutableView(as:).withUnsafeMutablePointer`'s pointer, and symmetrically `NDArray.View.withUnsafePointer`'s pointer handed straight to `MLXArray.init(_:UnsafeBufferPointer<T>,_:)` — exactly one copy per direction instead of two (no intermediate Swift `[Float16]` array). True zero-copy (`NDArray.MutableRawView.init(mutableBytes:)`/`RawView.init(bytes:)`, which accept a `RawSpan`/`MutableRawSpan` that could alias a caller buffer) was investigated and rejected: this SDK documents nothing about whether Core AI retains/copies/mutates such a buffer past the call, too risky for a K/V buffer read on every one of a chunk's 64 evaluations. Numerically bit-exact before/after. **(2) No native int8 weight-compression preset exists in `coreai-torch` 0.4.2 / `coreai-core` 1.0.0b2** distinct from the custom `torch.ops.coreai.{quantize,dequantize}` op route T-6.4 already used — checked exhaustively (`coreai_torch`'s full public API, every pass/pipeline enum in `coreai._compiler._transforms.passes`, `AIProgram.optimize()`'s implementation, `xcrun coreai-build compile --help`). Anyone hoping for a "just flip a preset flag" int8 path at this SDK version will not find one; the custom-op route is the only one available. **(3) an unquantized fp16 NAR-stack `.aimodel` is impractical to specialize on this hardware**: first-time specialization of a 2.6 GB fp16 asset (vs. 1.3 GB for the int8 one) drove one process to ~63 GB resident memory and 93% system swap usage (11.3/12.2 GB) in over 5 minutes without finishing a single shape's timing loop — killed to protect the machine. This is a real, reproducible resource cost independent of any runtime-speed question, and by itself would rule out shipping an unquantized fp16 Core AI asset regardless of `coreai-opt`'s existence. **(4) Final measurement, rigorous protocol (120s cooldown before each point, A/B/B/A, judged against a ±2% control)**: MLX's own NAR `velocity` timing is extremely stable (≤0.1% spread across repeats) — trustworthy signal. Core AI GPU is noisier (up to ~14% spread at the larger shape, consistent with CLAUDE.md's GPU-clock-state warning) but the GPU/MLX ratio is unambiguous regardless: 5.2-8.3× slower across every point and every repeat, after fix (1) already delivered a genuine ~4-7× improvement to the once-per-chunk K/V conversion step specifically (the "before" single-sample numbers were themselves internally inconsistent — 4785ms vs 2588ms for two shapes that convert an identically-sized K/V buffer — while the "after" numbers agree within ~1-2% across repeats, which is itself the strongest evidence of a real fix, more than the raw before/after delta). Padding `L_ar` up to the next bucket (512→1024, ~2× the real AR length) showed no throughput penalty distinguishable from measurement noise (+1.3% conversion, +1.9-2.9% per-eval) — the "risque L_ar" from `plan/13-ios-backend.md` §13.7 does not manifest here because per-eval cost is dominated by fixed Core AI runtime/dispatch overhead, not by attention compute scaling with K length. Per Vincent's stated rule (Core AI GPU > 2× MLX ⇒ no NAR asset in the iPhone bench pack), **T-6.10 is closed** without waiting for G-9 — the gap is large enough (5-8×, not a close call) to decide on the Mac measurement alone.
- 2026-09-21 — T-6.5 (partie app, `../yue2-ios` A-0.0/A-0.1) : `YuE2Core` compile et se lie pour l'iPhone (Release, signé) et le simulateur iOS 27 **à condition de ne pas lier CoreAI explicitement** : le SDK iPhoneSimulator 27.0 ne contient pas `CoreAI.framework` (présent dans les SDK iPhoneOS et macOS). `Package.swift` : `linkedFramework("CoreAI")` retiré, l'auto-link suffit partout où `import CoreAI` compile. x86_64 exclu des builds simulateur côté app (Mac Apple Silicon, inutile).
- 2026-09-21 — Simulateur iOS, au-delà du lien CoreAI ci-dessus : **MLX plante au lancement dès la première `MLXArray`, indépendamment de Core AI**. Vérifié directement (hors fiche, question de Vincent « ça tourne au moins dans le simulateur ? ») : `xcodebuild -scheme yue2 -destination 'platform=iOS Simulator,id=<iPhone 18 Pro Max, iOS 27.0>' build` compile et signe (`Sign to Run Locally`) sans problème ; `xcrun simctl spawn <udid> <bin> info` (aucun MLX touché) s'exécute et affiche la sortie attendue ; mais `xcrun simctl spawn <udid> <bin> decode --latents … --out …` (première `MLXArray` réelle, VAE fp32 réel) plante à coup sûr, reproduit deux fois : `SIGABRT`, `libc++ Hardening assertion __s != nullptr failed: basic_string(const char*) detected nullptr`. Rapport de crash (`~/Library/Logs/DiagnosticReports/yue2-*.ips`) symbolisé : `DecodeCommand.run()` → `MLXArray.init` → `mlx_array_new_data` → `mlx::core::allocator::malloc` → `mlx::core::metal::allocator()`/`MetalAllocator::MetalAllocator()` → `mlx::core::metal::device(...)` → **`mlx::core::metal::Device::Device()`** → `std::string(char const*)` avec un pointeur nul. C'est le constructeur du wrapper Metal de mlx-core (bibliothèque C++ `Cmlx`, pas notre code) qui construit une `std::string` à partir d'une chaîne C que Metal renvoie `nullptr` dans l'environnement simulé (très probablement un descripteur de `MTLDevice`, ex. son nom, absent/incomplet sous le Metal logiciel du Simulator) — cohérent avec les limites documentées de longue date de Metal-en-Simulateur, pas un bug de ce port. **Conclusion pratique** : le Simulator iOS ne peut valider que du code qui ne touche jamais MLX (arguments, chemins de fichiers, logique CLI) ; toute vérification touchant réellement l'inférence (VAE, NAR, AR) nécessite un appareil physique — aucun raccourci Simulator possible pour anticiper T-6.5/G-9. Non creusé plus loin (pas de session lldb complète) : le check est jugé suffisant pour trancher la question posée, pas pour corriger mlx-swift/mlx-core.
- 2026-09-21 — **Mémoire iPhone, hors fiche (demande de Vincent : « ils ont peut-être réfuté trop vite Core AI, n'écarte pas cette piste ; mesure aussi les temps »)**. Détails durables dans `decisions/stage-scoped-weight-residency.md` et `pitfalls/coreai-enumerated-shape-zero-padding.md`. **(1) Le pic n'est pas où la note « YuE2 Memory Budget » le plaçait.** Traces `swift-mlx-profiler` de la chanson de 64 s (`.local-runs/song-full-int4mixed-profiled`, `-profiled` bf16) : le NAR plafonne à 4 374 Mo actifs (int4-mixed) / 8 292 (bf16) ; c'est le **décodage VAE** (fp32, tuile 1024) qui porte le pic : 9 113 Mo actifs / 10 319 Mo process (int4-mixed), 12 791 / 14 242 (bf16) — +5,5 Go d'activations transitoires au-dessus de poids LM que plus rien ne lit. Charge (int4-mixed) : NAR 1 428 Mo, AR 756, `lm_head` 722 (bf16 sans `--quant-head`), `embed_tokens` 203. **(2) VAE seul, `yue2 decode` sur le latent réel (1 599 frames), pics de décodage seuls (échantillonneur 5 ms, chargement exclu), profil mobile** : MLX fp32/1024 : 5 498 Mo actifs, 5 293 process, 1,38 s ; MLX fp16/1024 : 2 871 / 3 217, 0,95 s ; MLX fp16/256 : 2 057 / 2 420, 1,01 s (A/B/B/A : 1,01 / 1,01) ; fp16/128 : 1 731 ; /64 : 1 358 ; /32 : 1 017 (1,86 s) — MLX garde un plancher ≈ 1 Go même à 32 frames (implémentation des convolutions, non creusé) ; **Core AI GPU** : 53 Mo MLX, 2 021-2 123 Mo process quelle que soit la tuile (plan mémoire statique : 1 785 Mo dès le chargement, +230 Mo à l'exécution), **1,04-1,07 s** (A/B/B/A : 1,05 / 1,04) — vitesse égale à MLX, footprint plus bas de 0,4 Go (fp16/256) à 3,3 Go (fp32/1024). **(3) Parité Core AI VAE réparée** : le 33,75 dB tuilé de T-6.3 était le zéro-padding des latents de la dernière tuile (92/288 frames réelles → 20,7 dB) ; fenêtres à forme exacte (`planVAETiles`) → `vae_coreai snr=54,7 dB`, pire tuile 53,3, pire couture 54,9, identique pour les tuiles 1024 et 256, `PARITY OK`. Core AI GPU est donc un backend VAE valide ; ANE inchangé. **(4) NAR, `bench-coreai-nar --quant int4-mixed`, profil mobile, L_ar 512, 6 évaluations, 120 s de refroidissement, deux ordres (A/B/B/A)** : MLX 140 / 139 ms par évaluation à L=256, 489 / 680 ms à L=1024 (dispersion 30 % sur ce point — un serveur LLM tiers tournait sur la machine, chiffres « en session ») ; Core AI GPU 835 / 779 ms (L=256), 3 406 / 3 501 ms (L=1024) : **5-7× plus lent**, confirme T-6.4b. Mémoire : base 3 138 Mo (LM int4-mixed entier) ; MLX ajoute +250 à +520 Mo pendant les évaluations ; Core AI ajoute +860 Mo au chargement et +160 Mo à l'exécution (`vmmap` : l'asset de 1,3 Go est mappé propre — 1 347 Mo « mapped file » non comptés dans le footprint — les poids exécutables résident en `IOAccelerator` dirty). Étape NAR seule : MLX ≈ 1,43 Go de poids + 0,3-0,5 Go ≈ 1,8-1,9 Go ; Core AI ≈ 1,0 Go → **≈ 0,9 Go de moins, pour 5-7× le temps** ; limite : `L_BUCKETS` s'arrête à 1 536 frames, la chanson de 64 s (1 601) ne passe pas sans un palier 2 048. **(5) Résidence par étape implémentée en MLX** (`WeightResidency.swift`, `LMWeightLoader.load(residency:)`, `ModelSession.loadWeights(of:)`, `YuE2Pipeline(releaseWeightsBetweenStages:)`, `Synthesizer.onBeforeChunk(needsARPath:)`, `YUE2_MEMORY_PROFILE=mobile`) : chanson de 64 s, int4-mixed, profil Mac, VAE fp16/256 → pic process **10 319 → 6 438 Mo** (phase AR : 3,5 Go de poids + 2 Go de cache), NAR 2 551 Mo actifs / 3 816 process, VAE 1 092 actifs / 2 290 process ; le pack est bit-exact (tests `WeightResidencyTests`, `VAETilingTests`). Prochaine marche : AR-seul au chargement (fait : `residency: [.ar]`) + `--quant-head` + cache mobile → max(étapes) ≈ 2,6-2,9 Go, à mesurer sur l'iPhone (T-6.5). **(6) Chanson complète, A/B/B/A (120 s de refroidissement avant chaque run, `.local-runs/ab-*`)** — A = int4-mixed tel quel (fp32, tuile 1024, tout résident), B = résidence par étape + AR seul au chargement + VAE fp16/256 : pic process A **9 036 / 9 009 Mo** (VAE) → B **5 002 / 5 002 Mo** (phase sémantique : 2,2 Go de poids AR + 2 Go de cache Mac + KV) ; NAR B 3 211 actifs / 4 470 process ; VAE B 1 092 / 2 292. Temps : A 110,5 s (nar 75,9 — premier run après build+tests, GPU chaud, à écarter) et 76,9 s (nar 55,1) ; B 86,9 s (nar 63,6) et 85,0 s (nar 63,0). Les deux B se répètent à 1 % ; les deux A s'écartent de 38 % : l'écart NAR B − A (63 vs 55 s, +15 %) reste **sous la dispersion du contrôle**, donc non démontré — piste plausible si réel : cache MLX vidé à l'échange AR → NAR (reconstruit pendant le solve) et chargement des 1,4 Go NAR en cours de run (≈ 1 s). Un serveur LLM tiers (27B) tournait sur la machine pendant toute la session : aucun de ces temps n'est une référence `BENCHMARKS.md`. **Scénarios iPhone à trancher à G-9** : « place disponible » = MLX partout (NAR 140 ms/évaluation à L=256, VAE fp16/256) avec résidence par étape, ≈ 2,6-2,9 Go ; « mémoire contrainte » = idem + VAE Core AI GPU (même vitesse, −0,4 Go sur l'étape VAE) et, si l'étape NAR ne rentre toujours pas, NAR Core AI GPU (−0,9 Go, 5-7× plus lent, palier 2 048 à exporter).

