# T-1.4 — Catalogue des modèles, téléchargement, `yue2 info`

Jalon : 1 · Prérequis : T-1.0 · Tier : 2 (réseau) · ⛔ GATE : non

## Objectif
`yue2 download` remplit `$YUE2_MODELS_DIR/{YuE2-3B,YuE2-Vae}` (+ `YuE2-Vae-legacy` optionnel, + `tokenizer.json` Qwen2.5) avec reprise et vérification SHA-256 ; `yue2 info` affiche ✓/✗ par fichier.

## À lire
- `plan/04-jalon1.md` ligne « 1.4 »
- `reference/gemma-4/Sources/Gemma4Swift/Download/Gemma4DownloadSession.swift` lignes 1-200 puis 200-363 (URLSession, `resolve/main`, reprise, vitesse)
- `reference/gemma-4/Sources/Gemma4Swift/Download/Gemma4DownloadTask.swift` (68 lignes)
- `reference/ltx/Sources/LTXVideo/Configuration/LTXModelCatalog.swift` lignes 1-80 (struct licence)

## Fichiers à créer
- `Sources/YuE2Core/Loading/ModelCatalog.swift` : `public enum YuE2Model: String, CaseIterable { case lm = "YuE2-3B", vae = "YuE2-Vae", vaeLegacy = "YuE2-Vae-legacy" }` avec `repoID` (`m-a-p/…`), `files: [String]` (LM : `config.json, generation_config.json, yue2_generation_config.json, weights_manifest.json, model.safetensors, qwen.tiktoken, examples/tonight-awake.json` ; VAE : `config.json, model.safetensors, weights_manifest.json`), `license` (`CC-BY-NC-4.0`, `allowsCommercialUse: false`, URL) ; entrée supplémentaire `tokenizerSource = ("Qwen/Qwen2.5-0.5B", "tokenizer.json", destination "YuE2-3B/tokenizer.json.qwen25")` (converti en T-1.5).
- `Sources/YuE2Core/Loading/ModelDownloader.swift` (≤ 200 l.) : `public actor ModelDownloader { init(modelsDir: URL, token: String? = ProcessInfo…["HF_TOKEN"]) ; func download(_ model: YuE2Model, progress: (DownloadProgress) -> Void) async throws ; func verify(_ model: YuE2Model) throws -> [String: Bool] }` — URL `https://huggingface.co/<repo>/resolve/main/<file>`, reprise par en-tête `Range` sur un `.part`, renommage atomique, SHA-256 comparé à `weights_manifest.json` (`CryptoKit`) pour `model.safetensors`.
- `Sources/YuE2CLI/DownloadCommand.swift`, `InfoCommand` mis à jour (tableau ✓/✗ par fichier, taille, SHA vérifié ✓ si `.sha256ok` présent).
- `Tests/YuE2Tests/CatalogTests.swift` (tier 1) : `YuE2Model.lm.files` contient `model.safetensors` et `qwen.tiktoken` ; URL construite exacte ; licence non commerciale.

## Étapes
1. Catalogue + test → build → tests.
2. Downloader (copie de gemma, renommée, sans `@Observable`), commande CLI.
3. `yue2 download --models-dir $YUE2_MODELS_DIR --model lm,vae` (7,8 Go : lancer en arrière-plan avec `nohup … > .local-runs/download.log &` puis vérifier avec `tail -3 .local-runs/download.log`, ne pas bloquer un pas d'agent plus de 10 min).
4. Commit `feat(loading): model catalog and resumable downloader`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh CatalogTests` → `TESTS OK (≥ 3 tests, 0 skippés)`
3. `bash -c '.xcodebuild/Build/Products/Release/yue2 info --models-dir "$YUE2_MODELS_DIR" | tail -12'` → chaque fichier LM et VAE marqué ✓ et `sha256 ✓` pour les deux `model.safetensors`

## Critère de fin
Validations vertes ; `$YUE2_MODELS_DIR/YuE2-3B/model.safetensors` = 7 261 441 640 octets, `YuE2-Vae/model.safetensors` = 530 512 720 octets.

## Pièges à cocher (plan/09-pieges.md)
n°16

## Entrée de journal
```
## T-1.4 — Catalogue et téléchargement — <date> — validée
- Fait : Loading/{ModelCatalog,ModelDownloader}.swift, CLI download/info, CatalogTests
- Bug réel rencontré : …
- Validation : … ; `yue2 info` → tous ✓
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
