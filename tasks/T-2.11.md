# T-2.11 — Profiler LLM + `yue2 profile` — ⛔ GATE G-3

Jalon : 2 · Prérequis : T-2.10 · Tier : 2 · ⛔ GATE : **G-3**

## Objectif
Mesurer TTFT, prefill tok/s, decode tok/s et mémoire avec `swift-mlx-profiler`, exporter une Chrome Trace. Recette unique : `plan/00.3-validation.md`.

## À lire
- `plan/00.3-validation.md` (bloc « Intégration du profiler »)
- `reference/gemma-4/Sources/Gemma4CLI/ProfileCommand.swift` lignes 60-140 et 200-220
- `reference/profiler/Sources/MLXProfiler/LLMProfiling.swift` lignes 110-200 (`startPrefill/endPrefill/startGeneration/endGeneration/getLLMMetrics`)

## Fichiers à créer / modifier
- `Sources/YuE2Core/Generation/TokenGenerator.swift` : appels `MLXProfiler.shared.startPrefill()/endPrefill()`, `startGeneration()/endGeneration(tokenCount:)`, `setTTFT` (no-op si le profiler est désactivé).
- `Sources/YuE2CLI/ProfileCommand.swift` : `yue2 profile plan|semantic --request … --out dir` : crée `ProfilingSession` (§0.3 point 1), phases `1. Model Loading`, `2. Tokenization`, `3. Prefill`, `4. Token Generation`, imprime `session.generateReport()` + `getLLMMetrics()` et écrit `trace.json`.
- `Tests/YuE2Tests/ProfilingTests.swift` (tier 1) : session avec deux phases ⇒ `phaseSummaries()` contient les deux noms, `durationMs > 0` ; `ChromeTraceExporter.export` non vide.

## Étapes
1. Implémenter ; lancer `yue2 profile semantic` (300 tokens) ; copier les chiffres dans `BENCHMARKS.md` (créer le fichier avec l'en-tête §3.3 et la première ligne) ; commit `feat(profiling): LLM metrics and chrome trace`.
2. Question G-3 dans `tasks/ASK.md` : démo `yue2 plan`/`yue2 semantic` + chiffres ; « on passe au Jalon 3 ? ».

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh ProfilingTests` → `TESTS OK (≥ 2 tests, 0 skippés)`
3. `bash -c '.xcodebuild/Build/Products/Release/yue2 profile semantic --plan .local-runs/plan --out .local-runs/prof --semantic-max-tokens 300 --semantic-min-tokens 0 --models-dir "$YUE2_MODELS_DIR" 2>&1 | grep -E "Prefill|Token Generation|TOTAL|tok/s" | head -6 && test -s .local-runs/prof/trace.json && echo TRACE OK'` → lignes de phases + `TRACE OK`

## Critère de fin
Validations vertes ; `BENCHMARKS.md` créé ; G-3 posée ; **terminer**.

## Pièges à cocher (plan/09-pieges.md)
n°13, 15

## Entrée de journal
```
## T-2.11 — Profiler LLM — <date> — validée (GATE G-3 posée)
- Fait : ProfileCommand, hooks profiler dans TokenGenerator, ProfilingTests, BENCHMARKS.md
- Bug réel rencontré : …
- Validation : … ; TTFT <s>, prefill <tok/s>, decode <tok/s>, pic <Go>
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
