# T-4.2 — O2 : réutiliser le cache KV sémantique pour le prefill NAR

Jalon : 4 · Prérequis : T-4.1 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
Quand un seul chunk et `guidance == 1`, le `KVCache` rempli par la phase sémantique (préfixe + tokens) sert de cache AR au NAR ; ajouter `MUSIC_END` par un pas de décodage (toujours, puisque le END est consommé sans passer dans le cache) et vérifier `cache.offset == arTokens.count`. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O2
- `reference/voxtral/Sources/VoxtralCore/TTS/VoxtralTTSModeling.swift` : `grep -n "cloneKVCaches" …`, 20 lignes
- `Sources/YuE2Core/Synthesis/CachedNAR.swift` (le vôtre)

## Fichiers à modifier
- `Synthesizer.synthesize(reuseCache:)` (activer), `SemanticGenerator` (retourner le cache), `YuE2Pipeline`
- `Tests/YuE2Tests/KVCacheReuseTests.swift` (tier 1, tiny) : velocity avec cache réutilisé == velocity avec prefill frais (1e-6) ; cas tronqué (END absent) ⇒ ajout du token END puis égalité

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O2 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-tests.sh KVCacheReuseTests` → `TESTS OK (≥ 2 tests, 0 skippés)` ; `Scripts/check-parity.sh nar` → `PARITY OK nar …`
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°2, 3

## Entrée de journal
```
## T-4.2 — O2 : réutiliser le cache KV sémantique pour le prefill NAR — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
