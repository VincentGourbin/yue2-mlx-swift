# T-2.10 — CLI `yue2 plan` et `yue2 semantic`

Jalon : 2 · Prérequis : T-2.9 · Tier : 2 · ⛔ GATE : non

## Objectif
Générer un plan ABC et des tokens sémantiques depuis la ligne de commande, avec progression et débit.

## À lire
- `reference/h3/Sources/MiniMaxH3CLI/MiniMaxH3CLI.swift` (37 lignes)
- `reference/yue/src/yue2/cli.py` lignes 86-140 (options de `generate`)

## Fichiers à créer
- `Sources/YuE2CLI/Options.swift` : `struct ModelOptions: ParsableArguments { @Option modelsDir (défaut env) ; @Option vae }`, `struct RequestOptions: ParsableArguments { --request <json> | --style/--lyrics ; --cot ; --seed ; --abc-file ; --cfg-scale ; --id }`, `struct SamplingOverrides` (`--abc-max-tokens`, `--semantic-max-tokens`, `--semantic-min-tokens`, `--temperature`…).
- `Sources/YuE2CLI/PlanCommand.swift` : charge modèle + tokenizer (une fois ; `YuE2MemoryManager.configure(.ar)`), `Planner.plan`, `plan.save(to: out)`, stderr : `planning score … <n> tokens · <tok/s>`.
- `Sources/YuE2CLI/SemanticCommand.swift` : `--plan <dir>` (`SymbolicPlan.load`), `generateSemantic`, écrit `semantic.npy` + `semantic.json` (timing, truncated) ; stderr : progression toutes les 100 tokens.
- `Sources/YuE2Core/Pipeline/ModelSession.swift` : `public final class ModelSession { static func load(modelsDir:, quant: … none) async throws ; let model, tokenizer, config }` (utilisée par toutes les commandes).

## Étapes
1. Implémenter ; exécuter sur `reference/yue/examples/song.json` ; noter les tok/s dans `docs/knowledge/log.md` ; commit `feat(cli): plan and semantic commands`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `bash -c '.xcodebuild/Build/Products/Release/yue2 plan --request reference/yue/examples/song.json --out .local-runs/plan --models-dir "$YUE2_MODELS_DIR" 2>&1 | tail -2 && head -8 .local-runs/plan/score.abc'` → ligne de débit + un en-tête ABC (`X:`, `L:`, `M:`, `K:` parmi les 8 premières lignes)
3. `bash -c '.xcodebuild/Build/Products/Release/yue2 semantic --plan .local-runs/plan --out .local-runs/song --semantic-max-tokens 300 --semantic-min-tokens 0 --models-dir "$YUE2_MODELS_DIR" 2>&1 | tail -1'` → ligne `semantic … tokens · <≥ 30> tok/s`

## Critère de fin
Validations vertes ; débit ≥ 30 tok/s bf16 (sinon noter et continuer : c'est le sujet de T-4.x).

## Pièges à cocher (plan/09-pieges.md)
n°13, 15, 16

## Entrée de journal
```
## T-2.10 — CLI plan/semantic — <date> — validée
- Fait : CLI Options/Plan/Semantic, Pipeline/ModelSession.swift
- Bug réel rencontré : …
- Validation : … ; ABC <n> tokens à <x> tok/s ; sémantique <y> tok/s
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
