# T-1.3 — Fixtures tiny (Python) + corpus tokenizer + protocole

Jalon : 1 · Prérequis : T-1.1, T-1.2 · Tier : 1 · ⛔ GATE : non

## Objectif
Générer, avec les classes **upstream** uniquement, les fixtures committées de `parity/` : `tiny_vae.safetensors`, `tiny_lm.safetensors`, `sampling.safetensors`, `protocol.json`, `tokenizer_corpus.json`. Sans elles, aucun module Swift de modèle ne se code.

## À lire
- `plan/08-tests.md` (tableau des contenus)
- `plan/annexe-A-fixtures.md` (squelette à compléter)
- `reference/yue/tests/test_nar.py` lignes 1-60 (fixture `model`, `dense_velocity`)
- `reference/yue/tests/test_vae.py` lignes 1-40 (`tiny_config`)
- `reference/yue/src/yue2/sampling.py` lignes 1-60 (`window_penalty`, `distribution`)
- `reference/yue/src/yue2/tokenization_yue2.py` lignes 1-41

## Fichiers à créer
- `Scripts/reference/tiny_fixtures.py` (≤ 200 lignes ; découper en `tiny_vae.py`, `tiny_lm.py`, `tiny_sampling.py`, `tiny_protocol.py` + `tiny_fixtures.py` qui les appelle si nécessaire).
- `parity/README.md` : 10 lignes — comment régénérer, interdiction d'éditer à la main.
- Les cinq fichiers de `parity/`.

## Étapes
1. VAE tiny (seed 231) : poids `decoder.*` + `expected_merged.<clé>.weight` (lire `module.weight` après `weight_norm`, c'est torch qui fusionne) + `input_T{1,15,17,33}` / `expected_full_T…` + taps `tap_T33.after_layer_{0..8}` (une boucle sur `model.decoder.layers`) + `expected_tiled_T17_core8`, `expected_tiled_T33_core16`.
2. LM tiny (seed 173, config de `test_nar.py`) : tout le `state_dict` ; `ids_full = [[3,5,8,7,4,9]]`, `logits_full` ; taps `hidden_after_layer_{0,1}` via hooks ; `logits_chunk_{0,1,2}` avec `StaticKVCache` et `cache_position` pour (0,2),(2,5),(5,6) ; `positions_explicit=[1,4,9,10]` sur `ids[:, :4]` → `logits_explicit` ; `rope_cos`, `rope_sin` pour positions `[0,1,4,9,10]` ; NAR : chunk `[2,3,4,5]`, `noise [5,64]` seed 42, `velocity_raw{20.0,0.0,-2.3}`, `time_emb_raw{…}`, taps `nar_after_vae2llm`, `nar_after_time`, `nar_after_pos`, `nar_after_layer_{0,1}` (instrumenter en recopiant `CachedNAR.velocity` dans le script) ; `solve4_noise [3,64]` seed 381, `solve4_expected` ; greedy : monkeypatch `yue2.sampling.{EOD,ABC_END,MUSIC_END,CODEC_OFFSET,CODEC_SIZE}` = 5,6,7,8,20, `generate_tokens(model, [2,3,4], Sampling(temperature=0, top_p=1, top_k=1, repetition_penalty=1, penalty_window=1, min_tokens=0, max_tokens=8), seed=0, phase="semantic", use_cuda_graph=False)` → `greedy_prefix`, `greedy_tokens`, `greedy_truncated` ; CFG tiny : même appel avec `negative=[2,3]`, `cfg_scale=1.5`, `temperature=0` → `cfg_tokens`.
3. Sampling : `logits = torch.randn(1, 184704, generator=seed 7)` ; 6 configs (ABC défaut step 40 history de 60 ids aléatoires dans [0,151643) ; sémantique défaut step 300 history 70 ids dans [151853, 184621) ; temperature 0 ; top_p 1 ; legacy_off (bf16, phase semantic, cot off) ; min_tokens actif (step 3)) → `cfg{i}_history`, `cfg{i}_step`, `cfg{i}_scores` (sortie de `distribution`), `cfg{i}_argmax` ; stocker les paramètres dans `metadata` en JSON.
4. Protocole : `protocol.json` = pour `examples/song.json` et `examples/tonight-awake.json` × cot × abc(none, `examples/score.abc`) : `text`, `prefix`, `negative` via `YuE2TextTokenizer(models_dir/"YuE2-3B"/"qwen.tiktoken")` **si** `$YUE2_MODELS_DIR` est défini, sinon via `hf_hub_download("m-a-p/YuE2-3B", "qwen.tiktoken")` (2,5 Mo).
5. Tokenizer corpus : ≥ 8 textes (song.json entier, 3 ABC, lyrics chinois de tonight-awake, instruction full, `"I'LL  <|im_start|> <abc> x:1\nK:C\n"`, `"café — naïve 🎵\n\n\n  deux  espaces"`) → `[{"text": …, "ids": […]}]` (NFC + `encode_ordinary`).
6. Chaque `save_file` avec `metadata={"kind", "seed", "upstream_sha", "generated"}`. Relancer deux fois : `shasum parity/*.safetensors` identiques.
7. Commit `test(parity): tiny fixtures from upstream classes`.

## Validation
1. `Scripts/check-fixtures.sh` → `FIXTURES OK`
2. `bash -c 'du -sh parity | cut -f1'` → taille < 5M
3. `bash -c '.venv-ref/bin/python Scripts/reference/tiny_fixtures.py >/dev/null && shasum parity/*.safetensors > /tmp/a && .venv-ref/bin/python Scripts/reference/tiny_fixtures.py >/dev/null && shasum parity/*.safetensors | diff - /tmp/a && echo DETERMINISTIC OK'` → `DETERMINISTIC OK`

## Critère de fin
Validations vertes ; `parity/README.md` présent.

## Pièges à cocher (plan/09-pieges.md)
n°4, 5, 6, 7, 16

## Entrée de journal
```
## T-1.3 — Fixtures tiny — <date> — validée
- Fait : Scripts/reference/tiny_*.py, parity/{tiny_vae,tiny_lm,sampling}.safetensors, protocol.json, tokenizer_corpus.json, parity/README.md
- Bug réel rencontré : …
- Validation : `Scripts/check-fixtures.sh` → `FIXTURES OK` ; déterminisme OK ; taille …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
