# T-2.1 — Config LM, RMSNorm, MLP, Attention (q/k-norm + RoPE)

Jalon : 2 · Prérequis : T-1.11 · Tier : 1 · ⛔ GATE : non

## Objectif
Les briques élémentaires de la voie transformer, copiées de flux-2 (Qwen3), avec q_norm/k_norm **avant** RoPE, RoPE non entrelacé, GQA via `MLXFast.scaledDotProductAttention`.

## À lire
- `plan/02.2-backbone-mot.md`
- `reference/flux-2/Sources/FluxTextEncoders/Model/Qwen3/Qwen3Attention.swift` (164 lignes : lire 1-164)
- `reference/flux-2/Sources/FluxTextEncoders/Model/Qwen3/Qwen3MLP.swift` (48 lignes)
- `reference/flux-2/Sources/FluxTextEncoders/Model/RMSNorm.swift` (31 lignes) — pour le fallback seulement

## Fichiers à créer
- `Sources/YuE2Core/Models/LM/YuE2Config.swift` : `public struct YuE2Config: Codable` (champs de §2.2 avec clés JSON `hidden_size`…) ; `load(from:)` ; `precondition(latentType == "vae")`.
- `Sources/YuE2Core/Models/LM/RMSNorm.swift` : `final class RMSNorm: Module, UnaryLayer { @ParameterInfo(key: "weight") var weight; init(dimensions:eps:) ; callAsFunction: MLXFast.rmsNorm(x, weight: weight.asType(x.dtype), eps: eps) }`.
- `Sources/YuE2Core/Models/LM/MLP.swift` : `gate_proj`, `up_proj`, `down_proj` (`Linear(bias: false)`), `down(silu(gate(x)) * up(x))`.
- `Sources/YuE2Core/Models/LM/Attention.swift` (≤ 200 l.) : `final class YuE2Attention: Module` avec `q_proj`, `k_proj`, `v_proj`, `o_proj`, `q_norm`, `k_norm` (RMSNorm(headDim)), `rope = RoPE(dimensions: headDim, traditional: false, base: ropeTheta)` ; `func projectQKV(_ x: MLXArray, offset: Int) -> (q, k, v)` : `q = q_norm(q_proj(x).reshaped(B, T, 16, 128))`, `k = k_norm(...)`, transposer en `[B, H, T, D]`, appliquer `rope(q, offset:)`, `rope(k, offset:)` ; `func attend(q, k, v, mask: MLXArray? | causal) -> MLXArray` : `MLXFast.scaledDotProductAttention(queries: q, keys: k, values: v, scale: 1/sqrt(128), mask: …)` puis `o_proj` après `[B, T, H·D]`.
- `Tests/YuE2Tests/BackboneParityTests.swift` (début) : suite `BackboneParityTests` ; test `ropeMatchesFixture` : `rope_cos/sin` de `parity/tiny_lm.safetensors` (head_dim 4) — appliquer `_apply_rotary` à un vecteur de référence… **plus simple** : appliquer le RoPE Swift à `q = ones([1,1,5,4])` positions [0,1,4,9,10] via `offset` par position et comparer à `[cos − sin, cos + sin]`-style attendu calculé depuis `rope_cos`, `rope_sin` (formule §2.2 : `x1·cos − x2·sin`, `x2·cos + x1·sin`).

## Étapes
1. Config → RMSNorm → MLP → Attention → test ; commit `feat(lm): attention, mlp, norms`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh BackboneParityTests` → `TESTS OK (≥ 1 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°1, 12

## Entrée de journal
```
## T-2.1 — Briques LM — <date> — validée
- Fait : Models/LM/{YuE2Config,RMSNorm,MLP,Attention}.swift, BackboneParityTests (rope)
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
