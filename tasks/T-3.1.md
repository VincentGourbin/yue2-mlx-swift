# T-3.1 — Modules NAR : time embedder, shiftT, positions, chunks

Jalon : 3 · Prérequis : T-2.11 · Tier : 1 · ⛔ GATE : non

## Objectif
Les briques NAR hors couches, validées sur le tiny : embedding de pas de temps, décalage sigmoid en bf16, lookup `pe`, découpage en chunks avec bruit injectable.

## À lire
- `plan/02.4-etape-nar.md`
- `reference/yue/src/yue2/modeling_yue2.py` : `grep -n "class TimestepEmbedder\|def _shift_t_value" reference/yue/src/yue2/modeling_yue2.py`, 20 lignes chacun
- `reference/yue/src/yue2/nar.py` lignes 20-45 (`Chunk`, `song_chunks`)

## Fichiers à créer
- `Sources/YuE2Core/Models/LM/NARModules.swift` : `extension YuE2ForCausalLM { func shiftT(raw: Double) -> MLXArray` (scalaire **bf16** : `sigmoid(MLXArray(Float(raw)).asType(dtype))`, puis formule shift = 1 ⇒ identité, garder la formule générale) ; `func timeEmbedding(tShifted: MLXArray) -> MLXArray [1, 2048]` (`freqs = exp(−ln(10000)·arange(128)/128)` fp32, `args = t.float()·freqs`, `[cos, sin]` → cast dtype → `mlp.fc1` → silu → `mlp.fc2`) ; `func latentPositions(count: Int) -> MLXArray` (`pe[0..<min(count, 24576)]`) }`.
- `Sources/YuE2Core/Synthesis/NARChunk.swift` : `public struct NARChunk { let arTokens: [Int]; let noise: MLXArray [T_c, 64] fp32 }` ; `static func songChunks(prefix:, codec:, seed: UInt64, context: Int = 24576, noise: MLXArray? = nil) throws -> [NARChunk]` (validation comme upstream ; bruit `MLXRandom.normal([T, 64], key: MLXRandom.key(seed))` si non injecté ; découpe par `chunkRanges`).
- `Tests/YuE2Tests/NARParityTests.swift` (début) : `timeEmbedding(shiftT(raw))` == `time_emb_raw{20,0,-2.3}` (1e-4, tiny fp32) ; `songChunks([2,3], [1]*11, context 15, noise injecté)` ⇒ 3 chunks de 5/5/1 frames, `arTokens` du chunk 1 == `[2,3,8+5…8+9,7]` avec constantes tiny.

## Étapes
1. Implémenter ; tests ; commit `feat(nar): timestep embedding, positions, chunks`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh NARParityTests` → `TESTS OK (≥ 3 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°3, 4

## Entrée de journal
```
## T-3.1 — Modules NAR — <date> — validée
- Fait : Models/LM/NARModules.swift, Synthesis/NARChunk.swift, NARParityTests
- Bug réel rencontré : …
- Validation : …
- Limites connues : bruit non identique au RNG torch (assumé, §12)
- Pas d'agent : … · appels d'outils : …
```
