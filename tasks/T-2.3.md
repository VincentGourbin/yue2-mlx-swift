# T-2.3 — Loader LM (628 tenseurs)

Jalon : 2 · Prérequis : T-2.2 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
Charger le vrai checkpoint bf16 en < 20 s sans aucune clé manquante ni inutilisée.

## À lire
- `plan/02.6-tenseurs.md` (déjà lu)
- `reference/h3/Sources/MiniMaxH3/Loading/WeightLoader.swift` lignes 88-150 (`loadTransformer` : chargement puis `apply`)

## Fichiers à créer
- `Sources/YuE2Core/Loading/LMWeightLoader.swift` : `static func load(directory: URL, config: YuE2Config) throws -> YuE2ForCausalLM` : `loadArrays(url: model.safetensors)` (mmap), aucun renommage (les clés Swift sont celles du checkpoint), `WeightLoader.apply` (diff bidirectionnel), puis `eval` **par tenseur** ; `YuE2MemoryManager.configure(for: .load)`.
- `Tests/YuE2Tests/LMLoaderTests.swift` : tier 1 : tiny se charge (déjà couvert) + test que `apply` refuse un checkpoint privé de `lm_head.weight` ; tier 2 (`RealLMLoaderTests`, `.enabled(if:)`) : chargement réel, `Memory.activeMemory` entre 7,0 et 7,8 Go, temps < 20 s (noté dans le journal, pas asserté strictement : `#expect(seconds < 60)`).

## Étapes
1. Implémenter ; commit `feat(loading): LM weight loader`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh LMLoaderTests` → `TESTS OK (≥ 2 tests, 0 skippés)`
3. `Scripts/check-tests.sh RealLMLoaderTests` → `TESTS OK (1 tests, 0 skippés)`

## Critère de fin
Validations vertes ; temps de chargement dans le journal.

## Pièges à cocher (plan/09-pieges.md)
n°8

## Entrée de journal
```
## T-2.3 — Loader LM — <date> — validée
- Fait : Loading/LMWeightLoader.swift, LMLoaderTests, RealLMLoaderTests
- Bug réel rencontré : …
- Validation : … ; chargement réel en <t> s, <Go> actifs
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
