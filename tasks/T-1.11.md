# T-1.11 — `yue2 decode` + gestion mémoire — ⛔ GATE G-1

Jalon : 1 · Prérequis : T-1.10, T-1.6 · Tier : 2 · ⛔ GATE : **G-1**

## Objectif
`yue2 decode --latents latent.npy --out audio.wav` décode un latent de référence en WAV écoutable ; `YuE2MemoryManager` fixe la limite de cache GPU par étape.

## À lire
- `reference/void/Sources/VOIDCore/Utils/VOIDMemoryManager.swift` (`find reference/void -name "VOIDMemoryManager.swift"` puis lire 100 lignes)
- `plan/07-jalon4.md` ligne O4

## Fichiers à créer
- `Sources/YuE2Core/Memory/YuE2MemoryManager.swift` : `public enum Stage { case load, ar, nar, vae }` ; `static func configure(for stage: Stage)` (`Memory.cacheLimit` : load 1 Go, ar 2 Go, nar 4 Go, vae 1 Go) ; `static func releaseBetweenStages()` (`Memory.clearCache()`) ; `static func snapshot() -> (activeMB, peakMB, cacheMB)`.
- `Sources/YuE2CLI/DecodeCommand.swift` : `yue2 decode --latents <npy> --out <wav> [--models-dir] [--vae standard|legacy] [--core-frames 1024] [--format int16|float32]` : lit `[T,64]`, `YuE2VAE.load`, `decodeTiled`, clamp, export, imprime `decoded <T> frames → <samples> samples (<s> s) in <t> s, peak <MB> MB`.
- `Tests/YuE2Tests/GenerationSmokeTests.swift` (tier 2, première partie) : `decode` d'un latent aléatoire seedé de 50 frames → WAV de `50·1920 − 64` frames, fini, `|x| ≤ 1`.

## Étapes
1. Obtenir un latent de référence : si `$YUE2_MODELS_DIR/parity/song/latent.npy` n'existe pas, le demander à Vincent (`tasks/ASK.md`) **ou** produire un latent court avec `real_fixtures.py song` si la sous-commande existe déjà (non, elle arrive en T-3.6) ⇒ utiliser à défaut `latent_40` de `parity/vae.safetensors` converti en `.npy` par un one-liner Python, et le noter.
2. Implémenter, tester, commit `feat(cli): decode latents to wav`.
3. Écrire la question G-1 dans `tasks/ASK.md` : « Démo : `yue2 decode` sur `<chemin>` ; écoute OK ? On passe au Jalon 2 ? » + le temps mesuré (3,6 min d'audio si un latent complet est disponible, sinon 40 frames) dans `docs/knowledge/log.md`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `bash -c '.xcodebuild/Build/Products/Release/yue2 decode --latents .local-runs/latent_40.npy --out .local-runs/decode_40.wav --models-dir "$YUE2_MODELS_DIR" | tail -1'` → ligne `decoded 40 frames → 76736 samples …`
3. `Scripts/check-tests.sh GenerationSmokeTests` → `TESTS OK (≥ 1 tests, 0 skippés)`

## Critère de fin
Validations vertes ; question G-1 écrite ; **terminer sans commencer T-2.1**.

## Pièges à cocher (plan/09-pieges.md)
n°11, 13, 16

## Entrée de journal
```
## T-1.11 — yue2 decode — <date> — validée (GATE G-1 posée)
- Fait : Memory/YuE2MemoryManager.swift, CLI decode, GenerationSmokeTests (partie decode)
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
