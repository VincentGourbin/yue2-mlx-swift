# T-1.8 — Décodeur VAE Oobleck + parité tiny + bissection

Jalon : 1 · Prérequis : T-1.7 · Tier : 1 · ⛔ GATE : non

## Objectif
`YuE2VAE.decode(latents [1, T, 64]) -> [1, 1920·T − 64, 2]` (NLC) en fp32, identique à la référence sur le modèle tiny, avec un test de bissection par bloc.

## À lire
- `plan/02.5-vae.md` (tableau de topologie)
- `reference/ltx/Sources/LTXVideo/Models/AudioVAE/BigVGANVocoder.swift` lignes 10-45 (`SnakeBeta`)
- `reference/yue/src/yue2/modeling_vae.py` : `grep -n "class ResidualUnit\|class DecoderBlock\|class OobleckDecoder" reference/yue/src/yue2/modeling_vae.py` puis lire 30 lignes à partir de chaque
- `reference/h3/Sources/MiniMaxH3/Models/AudioVAE/H3AudioVAEDecoder.swift` lignes 1-60 (style : Sequential fp32, clés `@ModuleInfo(key:)`)

## Fichiers à créer
- `Sources/YuE2Core/Models/VAE/VAEConfig.swift` : décode `config.json` (`decoder_config`, `latent_dim`, `downsampling_ratio`, `sample_rate`, `decode_core_frames`, `decode_halo_frames`, `release_variant`) ; `precondition(strides.reduce(1,*) == downsamplingRatio)`.
- `Sources/YuE2Core/Models/VAE/SnakeBeta.swift` : copie ltx (`alpha`, `beta` clés exactes, NLC).
- `Sources/YuE2Core/Models/VAE/OobleckDecoder.swift` (≤ 200 l.) : `ResidualUnit` (`layers` = `[SnakeBeta, Conv1d(k7, dilation d, padding 3d), SnakeBeta, Conv1d(k1)]` avec clés `layers.0..3`, sortie `x + layers(x)`), `DecoderBlock` (`layers.0` SnakeBeta, `layers.1` `ConvTransposed1d(kernel 2s, stride s, padding ceil(s/2))`, `layers.2..4` ResidualUnit dilations 1,3,9), `OobleckDecoder` (`layers.0` Conv1d(64→2048,k7,p3) ; `layers.1..6` DecoderBlock (2048→1024,s6),(1024→512,s5),(512→256,s4),(256→128,s4),(128→64,s2),(64→64,s2) ; `layers.7` SnakeBeta(64) ; `layers.8` Conv1d(64→2,k7,p3,**bias: false**)) ; `func callAsFunction(_ x, taps: ((Int, MLXArray) -> Void)? = nil)`.
- `Sources/YuE2Core/Models/VAE/YuE2VAE.swift` : `public final class YuE2VAE { let config; let decoder; static func load(directory: URL) throws` (config + `VAEWeightLoader.loadDecoderWeights` + `WeightLoader.apply`) ; `func decode(_ z: MLXArray) -> MLXArray` (vérifie `[1, T, 64]`, fp32, fini) ; `func naturalOutputLength(frames:) -> Int` = `1920·T − 64` }`.
- `Tests/YuE2Tests/VAEDecoderTests.swift` : charge le tiny depuis la fixture (**garde de couverture** : `apply` ne lève pas) ; `decode(input_T{1,15,17,33})` == `expected_full_T…` (max abs 1e-4) ; longueur == `1920·T − 64` ; bissection : taps `after_layer_0..8` sur `input_T33` vs `tap_T33.after_layer_i`, imprime `PARITY after_layer_i: max <e>` et échoue au premier > 1e-4.

## Étapes
1. Config + SnakeBeta → build ; OobleckDecoder → build ; YuE2VAE → build ; tests. Commit `feat(vae): Oobleck decoder with tiny parity`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh VAEDecoderTests` → `TESTS OK (≥ 6 tests, 0 skippés)`

## Critère de fin
Validations vertes. Si la bissection échoue au bloc 1 (première transposée) : vérifier layout `[out,k,in]` et `padding = ceil(s/2)` avant tout autre changement.

## Pièges à cocher (plan/09-pieges.md)
n°8, 9, 11

## Entrée de journal
```
## T-1.8 — Décodeur VAE — <date> — validée
- Fait : Models/VAE/{VAEConfig,SnakeBeta,OobleckDecoder,YuE2VAE}.swift, VAEDecoderTests.swift
- Bug réel rencontré : …
- Validation : …
- Limites connues : encodeur non porté
- Pas d'agent : … · appels d'outils : …
```
