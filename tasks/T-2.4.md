# T-2.4 — Prefill par blocs et positions explicites

Jalon : 2 · Prérequis : T-2.3 · Tier : 1 · ⛔ GATE : non

## Objectif
Un prefill découpé en blocs (2048) avec cache donne les mêmes logits que la passe pleine ; des positions explicites sont supportées.

## À lire
- `reference/voxtral/Sources/VoxtralCore/VoxtralModeling.swift` lignes 1160-1195 (prefill par blocs avec `eval` entre les blocs)

## Fichiers à créer / modifier
- `Sources/YuE2Core/Generation/TokenGenerator.swift` (partie 1) : `struct PrefillResult { let lastLogits: MLXArray; let cache: KVCache; let seconds: Double }` ; `static func prefill(model:, tokens: [Int], cache: KVCache = KVCache(), stepSize: Int = 2048, keepLastOnly: Bool = true) -> PrefillResult` : boucle par bloc, `forwardAR(block, cache:, keepLast: true)`, `eval(cache…)` après chaque bloc.
- `Attention.attend` : masque causal **aligné bas-droite** quand `T > 1` et cache non vide (`MLXFast.scaledDotProductAttention` avec `mask: .causal` gère l'alignement bas-droite pour `L_q < L_k` en mlx-swift 0.31 ; vérifier par le test, sinon construire un masque booléen `[T, L]` explicite).
- `Backbone.forwardAR(…, positions: MLXArray?)` : RoPE avec `offset: MLXArray` par position quand fourni.
- `Tests/YuE2Tests/BackboneParityTests.swift` : `logits_chunk_{0,1,2}` : blocs (0,2),(2,5),(5,6) sur `ids_full` avec un cache ⇒ concat == `logits_full` (1e-4) ; `positions_explicit` ⇒ `logits_explicit` (1e-4) ; décodage 1 token après prefill == colonne correspondante de `logits_full`.

## Étapes
1. Implémenter ; tests ; commit `feat(lm): chunked prefill, explicit positions`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh BackboneParityTests` → `TESTS OK (≥ 6 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°2, 12

## Entrée de journal
```
## T-2.4 — Prefill par blocs — <date> — validée
- Fait : Generation/TokenGenerator.swift (prefill), masque bas-droite, positions explicites, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
