# T-3.2 — CachedNAR : prefill du préfixe + velocity

Jalon : 3 · Prérequis : T-3.1 · Tier : 1 · ⛔ GATE : non

## Objectif
`velocity(state, rawT)` identique à la fixture tiny pour trois valeurs de t, avec taps de bissection.

## À lire
- `plan/02.4-etape-nar.md` (points 4-5)
- `reference/yue/src/yue2/nar.py` lignes 100-160 (`CachedNAR.__init__`, `_prefill`, `velocity`)

## Fichiers à créer
- `Sources/YuE2Core/Synthesis/CachedNAR.swift` (≤ 200 l.) : `public final class CachedNAR { init(model:, chunk: NARChunk, cache: KVCache? = nil, taps: …)` : si `cache == nil` : prefill voie AR de `chunk.arTokens` (réutiliser `TokenGenerator.prefill` par blocs) et garder `cache` ; `arLength`, `narLength = T_c + 2` ; précalcule `posEmb = latentPositions(narLength)` ; `func velocity(state: MLXArray [T_c,64], rawT: Double) -> MLXArray [T_c,64]` : `xNar = concatenated([zeros(1,64), state.asType(dtype), zeros(1,64)])`, `x = vae2llm(xNar)[None]`, `x += timeEmbedding(shiftT(rawT))`, `x += posEmb`, puis pour chaque couche : `q,k,v = layer.narSelfAttn.projectQKV(layer.narInputNorm(x), offset: arLength)` ; `K = concat(cache.keys[i], k, axis: 2)`, `V = …` ; `h = attend(q, K, V, mask: nil)` ; `x += narSelfAttn.oProj(h)` ; `x += narMlp(narPreMlpNorm(x))` ; taps optionnels ; retour `llm2vae(model.norm(x))[0, 1..<narLength−1]` }`.
- `DecoderLayer` : exposer `narSelfAttn`, `narInputNorm`, `narPreMlpNorm`, `narMlp` (déjà déclarés en T-2.2).
- `Tests/YuE2Tests/NARParityTests.swift` : `CachedNAR(model: tiny, chunk: [2,3,4,5] + nar_noise)` ; `velocity(nar_noise, raw)` == `velocity_raw{20,0,-2.3}` (1e-4) ; bissection `nar_after_vae2llm/time/pos/layer_{0,1}` sur raw 0.

## Étapes
1. Implémenter ; tests ; commit `feat(nar): cached prefix velocity`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh NARParityTests` → `TESTS OK (≥ 7 tests, 0 skippés)`

## Critère de fin
Validations vertes. Si `nar_after_pos` est bon mais `layer_0` diverge : vérifier l'offset RoPE (`arLength`) et l'absence de masque.

## Pièges à cocher (plan/09-pieges.md)
n°2, 3, 12

## Entrée de journal
```
## T-3.2 — CachedNAR — <date> — validée
- Fait : Synthesis/CachedNAR.swift, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
