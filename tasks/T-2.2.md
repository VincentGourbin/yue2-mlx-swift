# T-2.2 — DecoderLayer (deux voies), Backbone, KVCache, forwardAR

Jalon : 2 · Prérequis : T-2.1 · Tier : 1 · ⛔ GATE : non

## Objectif
Le modèle complet côté AR : logits pleine séquence identiques à la fixture tiny.

## À lire
- `plan/02.2-backbone-mot.md` (noms de clés)
- `reference/flux-2/Sources/FluxTextEncoders/Model/Qwen3/Qwen3DecoderLayer.swift` (50 lignes)
- `reference/flux-2/Sources/FluxTextEncoders/Model/Qwen3/Qwen3Model.swift` lignes 1-120 (structure Backbone) et 220-280 (lm_head)
- `reference/flux-2/Sources/Flux2Core/Transformer/TransformerKVCache.swift` (79 lignes)

## Fichiers à créer
- `Sources/YuE2Core/Models/LM/DecoderLayer.swift` : `@ModuleInfo(key: "input_layernorm") var inputNorm`, `self_attn`, `post_attention_layernorm`, `mlp`, `nar_input_layernorm`, `nar_self_attn`, `nar_pre_mlp_layernorm`, `nar_mlp` ; `func forwardAR(_ x, cache: KVCache?, layer: Int, mask:) -> MLXArray` (voie AR ; si cache : `k,v = cache.append(layer:, k:, v:)` puis `attend` sur tout le cache avec masque causal aligné en bas-droite si `T > 1` et cache non vide, sinon `.causal` pour un prefill sans cache, `nil` pour `T == 1`).
- `Sources/YuE2Core/Models/LM/KVCache.swift` : `public final class KVCache { var keys: [MLXArray?] ; var values: [MLXArray?] ; private(set) var offset = 0 ; func append(layer:k:v:) -> (MLXArray, MLXArray)` (concat sur l'axe 2, `[B,8,L,128]`, l'offset avance quand `layer == last`) ; `func clone() -> KVCache` ; `func truncate(to:)` }`.
- `Sources/YuE2Core/Models/LM/Backbone.swift` : `embed_tokens`, `layers`, `norm` ; `func forwardAR(tokens or embeds, cache:) -> MLXArray` (hidden normé) avec `taps: ((Int, MLXArray) -> Void)?` par couche.
- `Sources/YuE2Core/Models/LM/YuE2ForCausalLM.swift` : `model` (Backbone), `lm_head`, `vae2llm`, `llm2vae`, `time_embedder` (module `mlp` avec `Linear(256,2048)` clé `mlp.0`, `Linear(2048,2048)` clé `mlp.2`), `latent_pos_embed` (module avec `@ParameterInfo(key: "pe") var pe`) ; `func forwardAR(tokens: MLXArray, cache: KVCache?, keepLast: Bool) -> MLXArray` (logits `[B, T|1, V]`).
- `Tests/YuE2Tests/BackboneParityTests.swift` : charge le tiny (**couverture** : `WeightLoader.apply` ne lève pas — les modules NAR sont déclarés même si inutilisés ici) ; `logits_full` (1e-4) ; bissection `hidden_after_layer_{0,1}` (tap avant `norm`).

## Étapes
1. KVCache → DecoderLayer → Backbone → YuE2ForCausalLM → tests. Le module `time_embedder` peut être un `Module` avec `@ModuleInfo(key: "mlp") var mlp: Sequential`… **non** : MLX `Sequential` ne reproduit pas les clés `mlp.0`/`mlp.2` ; déclarer un `final class TimestepMLP: Module { @ModuleInfo(key: "0") var fc1: Linear; @ModuleInfo(key: "2") var fc2: Linear }` et `@ModuleInfo(key: "mlp") var mlp: TimestepMLP`.
2. Commit `feat(lm): MoT decoder layer, backbone, kv cache`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh BackboneParityTests` → `TESTS OK (≥ 3 tests, 0 skippés)`

## Critère de fin
Validations vertes ; 0 paramètre non alimenté sur le tiny.

## Pièges à cocher (plan/09-pieges.md)
n°1, 2, 12

## Entrée de journal
```
## T-2.2 — Backbone — <date> — validée
- Fait : Models/LM/{DecoderLayer,KVCache,Backbone,YuE2ForCausalLM}.swift, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : voie NAR déclarée, non exécutée
- Pas d'agent : … · appels d'outils : …
```
