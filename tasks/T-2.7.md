# T-2.7 — TokenGenerator : boucle de décodage, CFG, asyncEval

Jalon : 2 · Prérequis : T-2.6 · Tier : 1 · ⛔ GATE : non

## Objectif
`generate()` reproduit `generate_tokens()` : prefill, boucle, fin sur END, `minTokens`, `truncated`, CFG à deux caches, timings ; greedy tiny identique à la fixture.

## À lire
- `reference/yue/src/yue2/sampling.py` lignes 60-154 (`generate_tokens`)
- `reference/flux-2/Sources/FluxTextEncoders/Generation/Qwen3Generator.swift` lignes 80-100 et 205-230 (`asyncEval` du token suivant)

## Fichiers à créer / modifier
- `Sources/YuE2Core/Generation/TokenGenerator.swift` (partie 2, ≤ 200 l. ; sinon `TokenGenerator+CFG.swift`) : `public struct GenerationTiming { seconds, prefillSeconds, ttftSeconds, outputTokens, contentTokens, outputTPS, prefixTokens, cfgBranches }` ; `public struct GenerationResult { tokens: [Int] (globaux), timing, truncated }` ; `static func generate(model:, head: RestrictedHead, prefix: [Int], sampling:, seed: UInt64, bounds:, negative: [Int]? = nil, cfgScale: Double = 1, legacyOff: Bool = false, cache: KVCache? = nil (retourné rempli), onToken: ((Int) -> Void)?, cancel: (() -> Bool)?) throws -> GenerationResult` — garde `prefix.count + maxTokens ≤ 24576` ; `key = MLXRandom.key(seed)` puis `split` par pas ; CFG : `logits = uncond + scale·(cond − uncond)` en bf16 **avant** le processeur ; pipelining : calculer `next = sample(...)` **sans** `.item()`, `asyncEval(next)`, puis forward du pas suivant avec `next` comme entrée, lire `.item()` du pas précédent après avoir soumis le suivant ; le token END est consommé (non ajouté), `truncated = !eos`.
- `Tests/YuE2Tests/TokenGeneratorTests.swift` : tiny + bounds monkeypatchées (`eod 5, abcEnd 6, musicEnd 7, codecOffset 8, codecSize 20`) : `generate(prefix: greedy_prefix, temperature 0, maxTokens 8)` == `greedy_tokens`, `truncated == greedy_truncated` ; CFG : `negative=[2,3]`, `cfgScale 1.5`, greedy == `cfg_tokens` ; `minTokens` : avec `minTokens 3` le token END n'apparaît pas avant l'index 3 ; annulation : `cancel` vrai au 2e pas ⇒ `YuE2Error.cancelled`.

## Étapes
1. Sans CFG ni pipelining d'abord (tests greedy) ; puis CFG ; puis `asyncEval` (les tests doivent rester identiques) ; commit `feat(generation): token generator with CFG and async eval`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh TokenGeneratorTests` → `TESTS OK (≥ 4 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°6, 7

## Entrée de journal
```
## T-2.7 — TokenGenerator — <date> — validée
- Fait : Generation/TokenGenerator*.swift, TokenGeneratorTests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
