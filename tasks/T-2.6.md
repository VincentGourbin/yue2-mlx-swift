# T-2.6 — lm_head restreint par phase (O1, exact)

Jalon : 2 · Prérequis : T-2.5 · Tier : 1 (+2) · ⛔ GATE : non

## Objectif
Ne calculer que les lignes autorisées de `lm_head` par phase, avec table local→global ; résultat identique à la softmax masquée.

## À lire
- `plan/02.3-etape-ar.md` (dernier paragraphe, « Optimisation exacte ») et `plan/07-jalon4.md` ligne O1

## Fichiers à créer
- `Sources/YuE2Core/Models/LM/RestrictedHead.swift` : `final class RestrictedHead { let globalIDs: [Int]; let weight: MLXArray ([n, 2048]) ; init(lmHead: Linear, bounds: LogitsBounds)` (lignes `allowedRange` puis `endToken` en dernière position ; `take(lmHead.weight, MLXArray(globalIDs), axis: 0)`, `eval`) ; `func logits(hidden: MLXArray) -> MLXArray` (`matmul(hidden, weight.T)`) ; `func global(_ local: Int) -> Int` }`. `LogitsProcessor` accepte un mode « espace local » (bounds locales : `allowedRange = 0..<n−1`, `endToken = n−1`).
- `Tests/YuE2Tests/SamplingTests.swift` : sur le tiny : `RestrictedHead.logits` == `full_logits[globalIDs]` exactement ; `global(local(id)) == id` ; la chaîne complète (processor local + `global`) donne le même argmax que la chaîne globale sur `logits_full`.

## Étapes
1. Implémenter ; tests ; commit `perf(lm): restricted lm_head per phase`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh SamplingTests` → `TESTS OK (≥ 11 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°6

## Entrée de journal
```
## T-2.6 — lm_head restreint — <date> — validée
- Fait : Models/LM/RestrictedHead.swift, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : validation real en T-2.9
- Pas d'agent : … · appels d'outils : …
```
