# T-2.9 — Parité real du LM + `yue2 parity lm` — ⛔ GATE G-2

Jalon : 2 · Prérequis : T-2.8 · Tier : 2 · ⛔ GATE : **G-2**

## Objectif
Sur les vrais poids : logits du dernier token du préfixe et 8 tokens greedy (ABC et sémantique) identiques à la référence bf16 (MPS), avec bissection par couche.

## À lire
- `plan/08-tests.md` ligne `lm`
- `reference/yue/src/yue2/pipeline.py` : `grep -n "_load_model" reference/yue/src/yue2/pipeline.py` (chargement bf16), 15 lignes

## Fichiers à créer / modifier
- `Scripts/reference/real_fixtures.py` sous-commande `lm` : `YuE2ForCausalLM.from_pretrained(models_dir/"YuE2-3B", torch_dtype=bfloat16).to("mps").eval()`, `YuE2TextTokenizer`, `song.json` cot full : `prefix_abc = token_prefixes(...)` → `logits_last_abc` (fp32 stocké), hooks → `hidden_after_layer_{0,7,14,27}` ; `greedy_abc` = 8 tokens via `generate_tokens(..., Sampling(temperature=0, top_k=1, repetition_penalty=1, min_tokens=0, max_tokens=8), phase="abc", use_cuda_graph=False)` ; `prefix_semantic` avec `abc = examples/score.abc` → `logits_last_semantic`, `greedy_semantic` (8 tokens, phase semantic). Écrit `$YUE2_MODELS_DIR/parity/lm.safetensors` (`kind = "real_lm"`). Durée ≈ 2-5 min (première exécution MPS).
- `ParityCommand` : `yue2 parity lm` : `PARITY logits_abc: rel <e> argmax_equal <bool>`, `PARITY hidden_layer_{i}: rel <e>`, `PARITY greedy_abc: <k>/8 identical`, `PARITY greedy_semantic: <k>/8 identical`, verdict `PARITY OK lm …` si rel ≤ 2e-2, argmax égal, 8/8 et 8/8.
- `Tests/YuE2Tests/RealLMParityTests.swift` (tier 2) : mêmes assertions.

## Étapes
1. Générer la fixture (surveiller `.local-runs/real_lm.log`).
2. CLI + tests ; commit `feat(parity): real LM parity`.
3. Question G-2 dans `tasks/ASK.md` avec les valeurs mesurées (rel par tap, k/8).

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-parity.sh lm` → `PARITY OK lm …`
3. `Scripts/check-tests.sh RealLMParityTests` → `TESTS OK (≥ 3 tests, 0 skippés)`

## Critère de fin
Validations vertes ; G-2 posée ; **terminer**. Si `greedy_semantic` < 8/8 alors que logits rel < 2e-2 : c'est une différence bf16 sur un quasi-ex-æquo ; le noter, poser G-2 avec les deux candidats en compétition (valeurs de logits).

## Pièges à cocher (plan/09-pieges.md)
n°1, 2, 6, 7, 13

## Entrée de journal
```
## T-2.9 — Parité real LM — <date> — validée (GATE G-2 posée)
- Fait : real_fixtures.py lm, parity lm, RealLMParityTests
- Bug réel rencontré : …
- Validation : `PARITY OK lm …`
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
