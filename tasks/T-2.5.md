# T-2.5 — Processeur de logits (chaîne §2.3, pure MLX)

Jalon : 2 · Prérequis : T-2.4 · Tier : 1 · ⛔ GATE : non

## Objectif
Reproduire `distribution()` exactement, sans aller-retour hôte, validé sur `parity/sampling.safetensors`.

## À lire
- `plan/02.3-etape-ar.md` (ordre des 7 étapes, tableau)
- `reference/yue/src/yue2/sampling.py` lignes 1-60
- `reference/flux-2/Sources/FluxTextEncoders/Generation/Qwen3Generator.swift` lignes 410-445 (`sampleTopPGPU`)

## Fichiers à créer
- `Sources/YuE2Core/Generation/LogitsProcessor.swift` (≤ 200 l.) : `public struct LogitsBounds { let allowedRange: Range<Int>; let endToken: Int }` (ABC : `0..<eod`, `abcEnd` ; sémantique : `codecOffset..<codecOffset+codecSize`, `musicEnd` ; **paramétrable** pour le tiny) ; `public final class LogitsProcessor { init(sampling: Sampling, bounds: LogitsBounds, vocabSize: Int, legacyOff: Bool) ; var history: [Int] (fenêtre glissante des `penaltyWindow` derniers ids, maintenue aussi en MLXArray) ; func scores(logits: MLXArray [1,V], step: Int) -> MLXArray` : (1) `allowedMask` précalculé (`-inf` hors plage, 0 dedans + endToken) ; (2) `step < minTokens` ⇒ colonne end = `-inf` ; (3) pénalité : `ids = MLXArray(window)`, `counts = (ids[:,None] .== ids[None,:]).sum(axis: 1)`, `alpha = pow(penalty, counts)`, `vals = logits[ids]`, `new = where(vals < 0, vals * alpha, vals / alpha)`, `logits[ids] = new` ; (4) température (0 ⇒ retourner tel quel + flag `argmaxOnly`) ; (5) top-k : `threshold = sorted(scores, descending)[k−1]`, `where(scores < threshold, -inf, scores)` ; (6) top-p : `idx = argSort(-scores)`, `p = softmax(scores[idx])`, `removed = (cumsum(p) − p) > topP`, `removed[..<keep] = false` (keep = legacyOff ? 3 : 1), `vals = where(removed, -inf, scores[idx])`, scatter retour ; dtype fp32 (bf16 si `legacyOff`) ; `func sample(scores:, key:) -> Int` (`argMax` si temperature 0 sinon `MLXRandom.categorical(scores, key:)`) ; `func push(token:)` }`.
- `Tests/YuE2Tests/SamplingTests.swift` : pour chaque `cfg{i}` de la fixture : `scores` Swift == `cfg{i}_scores` (max abs 1e-5 sur les valeurs finies ; positions `-inf` identiques) et `argMax` == `cfg{i}_argmax` ; test pénalité vs implémentation CPU naïve en Swift sur 3 fenêtres avec doublons ; top-k conserve les égalités.

## Étapes
1. Implémenter par étape avec un test par étape ; commit `feat(generation): logits processor with parity`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh SamplingTests` → `TESTS OK (≥ 8 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°6, 7

## Entrée de journal
```
## T-2.5 — Processeur de logits — <date> — validée
- Fait : Generation/LogitsProcessor.swift, SamplingTests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
