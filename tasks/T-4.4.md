# T-4.4 — O7 : VAE — compile de SnakeBeta et ResidualUnit

Jalon : 4 · Prérequis : T-4.3 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
`compile(shapeless: true)` autour de l'activation SnakeBeta et de la chaîne élémentaire d'un ResidualUnit ; essayer aussi `coreFrames 2048`. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O7
- `reference/flux-2/Sources/Flux2Core/Transformer/Flux2FusedKernels.swift` lignes 1-40 (kill-switch par variable d'environnement — reprendre l'idée : `YUE2_DISABLE_COMPILE`)

## Fichiers à modifier
- `Sources/YuE2Core/Models/VAE/{SnakeBeta,OobleckDecoder}.swift`

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O7 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-tests.sh VAEDecoderTests` → `TESTS OK (…)` ; `Scripts/check-parity.sh vae` → `PARITY OK vae …`
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°11, 14

## Entrée de journal
```
## T-4.4 — O7 : VAE — compile de SnakeBeta et ResidualUnit — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
