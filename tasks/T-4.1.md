# T-4.1 — O4 : limite de cache GPU par étape

Jalon : 4 · Prérequis : T-4.0 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
Fixer `Memory.cacheLimit` par étape (AR 2 Go, NAR 4 Go, VAE 1 Go) et `clearCache()` entre étapes ; vérifier le pic mémoire. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O4
- `reference/ltx/Sources/LTXVideo/Utils/LTXMemoryManager.swift` (98 lignes)

## Fichiers à modifier
- `Sources/YuE2Core/Memory/YuE2MemoryManager.swift` (valeurs), appels dans `YuE2Pipeline`

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O4 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-parity.sh vae` → `PARITY OK vae …`
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°8

## Entrée de journal
```
## T-4.1 — O4 : limite de cache GPU par étape — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
