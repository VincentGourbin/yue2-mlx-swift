# T-6.5 — E5 : projet Xcode (manuel) + écran Bench sur l'iPhone — ⛔ GATE G-9

Jalon : 6 · Prérequis : T-6.4 · Tier : 2 (appareil) · ⛔ GATE : **G-9**

## Objectif
Les vrais chiffres de l'iPhone 15 Pro Max : chargement int4, décodage AR, `velocity` NAR en MLX et en Core AI (GPU, ANE), tuile VAE MLX et Core AI, mémoire, thermique.

## À lire
- `plan/13-ios-backend.md` §13.9 (procédure Xcode — **c'est Vincent qui crée le projet**) et §13.5 ligne E5
- `Sources/YuE2BenchUI/MetricsPanel.swift` (71 lignes)

## Où vit l'app
Décision du 2026-09-20 : l'app iOS est un **projet séparé**, `../yue2-ios` (YuE2 Studio). L'écran Bench de cette fiche y est réalisé par la fiche `yue2-ios/tasks/A-0.1.md` ; ce dépôt ne contient plus `Apps/`. Cette fiche se termine quand le JSON de mesures est rapatrié et G-9 posée ici.

## Répartition
- **Vincent** : crée `Apps/YuE2Mobile/YuE2Mobile.xcodeproj` selon §13.9 (étapes 1-6), commit du projet sans `Signing.xcconfig`, puis exécute le bench et rapatrie le JSON.
- **Agent** : fournit les fichiers Swift de l'app et `Scripts/ios-build.sh`, écrit la question G-9.

## Fichiers à créer (agent)
- `Apps/YuE2Mobile/YuE2Mobile/YuE2MobileApp.swift`, `BenchView.swift`, `BenchRunner.swift` : bouton « Bench » : (1) charge `Caches/models/YuE2-3B/mlx-prequantized/int4-all` (déposé via Finder / `devicectl device copy to`), temps et mémoire ; (2) 64 tokens greedy phase ABC sur `song.json` → tok/s, TTFT ; (3) 4 évaluations `velocity` sur 256 puis 1 024 frames, backends `mlx`, `coreai-gpu`, `coreai-ane` (assets iOS de T-6.3/T-6.4 dans *Resources*) → ms/évaluation, extrapolation NAR 32 pas ; (4) une tuile VAE 288 frames en MLX fp16 et Core AI GPU/ANE → ms ; (5) `ProcessInfo.thermalState` avant/après, pic mémoire (`SystemMetrics.processFootprint`) ; résultats affichés et écrits dans `Documents/bench-<date>.json`.
- `Scripts/ios-build.sh` (§13.9 étape 9) : build + install, verdict `IOS APP INSTALLED` ; `Scripts/ios-pull.sh <fichier>` : `devicectl device copy from`.
- `.gitignore` : `Apps/YuE2Mobile/Signing.xcconfig`, `Apps/**/xcuserdata/`.

## Étapes
1. Agent : fichiers Swift + scripts ; STOP et demande à Vincent de créer le projet (ASK « projet Xcode prêt ? »). 2. Vincent : projet, `Scripts/ios-build.sh`, dépôt du pack et des assets, bench, `Scripts/ios-pull.sh bench-*.json`. 3. Agent : tableau dans `docs/knowledge/log.md` ; ⛔ **G-9** dans `tasks/ASK.md` avec le tableau et une proposition (durée max, ODE, backends et placement, E6 oui/non).

## Validation
1. `bash -c 'YUE2_DEVICE_UDID=00008130-000235E436F0001C Scripts/ios-build.sh | tail -1'` → `IOS APP INSTALLED`
2. `.local-runs/bench-*.json` rapatrié avec les mesures (1)-(5) non nulles

## Critère de fin
Mesures rapatriées ; G-9 posée ; **terminer**.

## Pièges à cocher (plan/09-pieges.md)
n°15, 16
