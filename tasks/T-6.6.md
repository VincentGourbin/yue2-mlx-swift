# T-6.6 — `YuE2Runtime` + profil mémoire et thermique iOS

Jalon : 6 · Prérequis : T-6.5 (G-9 répondue) · Tier : 1 · ⛔ GATE : non

## Objectif
Une configuration unique qui porte les décisions de G-9 (précision, preset, backends, pas d'ODE, longueur max) et le comportement iOS (limites mémoire, pacing thermique, annulation).

## À lire
- `plan/13-ios-backend.md` §13.4 (principes) ; réponse G-9 dans `tasks/ASK.md`
- `Sources/YuE2Core/Pipeline/ModelSession.swift`, `Memory/YuE2MemoryManager.swift`

## Fichiers à créer / modifier
- `Sources/YuE2Core/Runtime/YuE2Runtime.swift` : `struct YuE2Runtime: Codable { precision, quant, vaeBackend: .mlx|.coreml|.auto, narDenseBackend, odeSteps, maxSemanticTokens, tileFrames, thermalPacing: Bool }` + `static let mac`, `static let mobile` (valeurs de G-9) ; kill-switches env `YUE2_DISABLE_COREML`, `YUE2_DISABLE_COMPILE`.
- `ModelSession.load(runtime:)`, `YuE2Pipeline(runtime:)`, `Synthesizer` : pause entre pas d'ODE si `ProcessInfo.thermalState ≥ .serious` (`Task.sleep` 2 s, journalisé), annulation coopérative déjà présente.
- `YuE2MemoryManager.mobile` : `cacheLimit` 256 Mo, `GPU.set(memoryLimit: 3 Go, relaxed: true)`, `clearCache()` entre étapes.
- CLI : `--runtime mac|mobile` pour tester le profil mobile sur Mac.
- Tests tier 1 : `YuE2Runtime` encode/décode ; pacing déclenché par un état thermique injecté.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK` ; `Scripts/check-build-ios.sh` → `BUILD IOS OK`
2. `Scripts/check-tests.sh RuntimeTests` → `TESTS OK (≥ 3 tests, 0 skippés)`
3. `bash -c '.xcodebuild/Build/Products/Release/yue2 generate --request reference/yue/examples/song.json --runtime mobile --out .local-runs/song-mobile-profile --abc-max-tokens 64 --semantic-max-tokens 100 --semantic-min-tokens 0 --models-dir "$YUE2_MODELS_DIR" 2>&1 | tail -1'` → ligne `generated …`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°8, 14
