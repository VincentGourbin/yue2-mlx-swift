# T-3.7 — Profiler TTS, `--profile`, BENCHMARKS.md

Jalon : 3 · Prérequis : T-3.6 · Tier : 2 · ⛔ GATE : non

## Objectif
Les quatre étapes mesurées par `TTSMetrics` (recette §0.3), trace Perfetto, première ligne complète de `BENCHMARKS.md`.

## À lire
- `plan/00.3-validation.md` (bloc « Intégration du profiler ») et `plan/03-budget.md` (§3.3, colonnes)
- `reference/voxtral/Sources/VoxtralCore/TTS/Pipeline/VoxtralTTSPipeline.swift` lignes 205-240 (appels `startSemanticGen`… en situation)

## Fichiers à modifier
- `Sources/YuE2Core/Pipeline/YuE2Pipeline.swift` : phases `1. Model Loading` (dans `ModelSession.load`), `2. Plan (ABC)`, `startSemanticGen/endSemanticGen(frameCount:)`, `startFlowMatching/endFlowMatching`, `startCodecDecode/endCodecDecode`, `setTTFT`, `setAudioDuration` ; sous-phases `ODE step k` (déjà T-3.3) et `VAE tile i`.
- `Sources/YuE2CLI/GenerateCommand.swift` : `--profile` ⇒ session, `generateReport()`, `getTTSMetrics().summary`, `trace.json` dans `--out`.
- `BENCHMARKS.md` : en-tête « Comment lire / comment contribuer » (10 lignes, modèle `reference/gemma-4/BENCHMARKS.md` lignes 1-40) + tableau §3.3 avec la ligne de la chanson complète (chiffres **copiés** de `TTSMetrics.summary` et du rapport de phases).

## Étapes
1. Implémenter ; `yue2 generate --profile` sur la chanson courte ; ligne dans `BENCHMARKS.md` (courte) ; commit `feat(profiling): TTS metrics and benchmarks table`.
2. Si le run complet de T-3.6 est terminé : ajouter sa ligne aussi.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `bash -c '.xcodebuild/Build/Products/Release/yue2 generate --request reference/yue/examples/song.json --out .local-runs/p1 --profile --abc-max-tokens 64 --semantic-max-tokens 100 --semantic-min-tokens 0 --ode-steps 4 --models-dir "$YUE2_MODELS_DIR" 2>&1 | grep -E "Model Loading|Plan \(ABC\)|Semantic Code Generation|Flow Matching|Codec Decode|RT factor" | head -8 && test -s .local-runs/p1/trace.json && echo TRACE OK'` → les 5 phases + `RT factor` + `TRACE OK`
3. `bash -c 'grep -c "^| 20" BENCHMARKS.md'` → `≥ 1`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°13, 15

## Entrée de journal
```
## T-3.7 — Profiler TTS — <date> — validée
- Fait : hooks TTS dans le pipeline, --profile, BENCHMARKS.md
- Bug réel rencontré : …
- Validation : … ; RT factor <x>
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
