# T-4.5 — O5 : quantification 8-bit affine de la voie AR (opt-in)

Jalon : 4 · Prérequis : T-4.4 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
Preset `--quant none|qint8|int4` appliqué aux seuls `Linear` des chemins `self_attn.*_proj` et `mlp.*` (et `lm_head` si `--quant-head`), exclusions : `nar_*`, `embed_tokens`, `*norm*`, `vae2llm`, `llm2vae`, `time_embedder`, `latent_pos_embed` ; export/chargement pré-quantifié sous `$YUE2_MODELS_DIR/YuE2-3B/mlx-prequantized/<quant>/`. Autorisé par G-5 seulement. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O5
- `reference/h3/Sources/MiniMaxH3/Configuration/QuantizationConfig.swift` (71 lignes, à copier)
- `reference/h3/Sources/MiniMaxH3/Loading/PrequantizedCheckpoint.swift` (95 lignes)
- `plan/09-pieges.md` n°10 (dtype des poids quantifiés)

## Fichiers à modifier
- `Sources/YuE2Core/Configuration/QuantizationConfig.swift`, `LMWeightLoader` (+ `RestrictedHead` : construire depuis le poids **avant** quantification ou dé-quantifier les lignes), `ModelSession.load(quant:)`, CLI `--quant`
- `Tests/YuE2Tests/QuantizationTests.swift` (tier 2) : logits rel < 5e-2 vs bf16 sur le préfixe de `lm.safetensors` ; ≥ 7/8 tokens greedy identiques

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O5 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-tests.sh QuantizationTests` → `TESTS OK (…)` ; écoute de Vincent (question dans `tasks/ASK.md` avec le WAV qint8 vs bf16)
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°8, 10

## Entrée de journal
```
## T-4.5 — O5 : quantification 8-bit affine de la voie AR (opt-in) — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
