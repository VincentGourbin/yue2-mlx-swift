# T-4.7 — O10 : GUI de bench (SwiftUI) — ⛔ GATE G-8

Jalon : 4 · Prérequis : T-4.6 · Tier : 2 · ⛔ GATE : **G-8**

## Objectif
`yue2-bench-ui` : onglet Génération (style, lyrics, cot, seed, ABC, cfg, quant ; progression par étape ; lecture audio ; export), panneau Mesures (`TTSMetrics` + mémoire, source unique = profiler), onglet Historique (table + CSV), bouton « Export trace ».

## À lire
- `plan/07-jalon4.md` ligne O10 et `reference/qwen38/PLAN.md` : `grep -n "### 4.3" reference/qwen38/PLAN.md`, lire 40 lignes (spec GUI)
- `reference/gemma-4/Sources/Gemma4BenchUI/ContentView.swift` lignes 1-120 et 330-360 (TabView, presets)

## Fichiers à créer (chacun ≤ 200 lignes)
- `Sources/YuE2BenchUI/YuE2BenchUIApp.swift` (TabView), `GenerationView.swift`, `GenerationViewModel.swift` (`@Observable @MainActor`, appelle `YuE2Pipeline` sur une `Task` détachée, `PipelineEvent` → progression), `MetricsPanel.swift`, `HistoryView.swift` (+ export CSV), `AudioPlayer.swift` (`AVAudioPlayer`), `ModelPicker.swift` (variants présents sur disque : bf16, prequantized).

## Étapes
1. Une vue à la fois, build après chaque ; démo interne : chanson courte depuis la GUI ; commit `feat(ui): bench app`.
2. `tasks/ASK.md` G-8 : démo GUI + `BENCHMARKS.md` ; « suite : encodeur VAE / serveur / iOS ? ».

## Validation
1. `Scripts/check-build.sh yue2-bench-ui` → `BUILD OK`
2. `Scripts/check-build.sh` → `BUILD OK`
3. `Scripts/check-tests.sh` → `TESTS OK (…)`

## Critère de fin
Validations vertes ; G-8 posée ; **terminer**.

## Pièges à cocher (plan/09-pieges.md)
n°14, 15, 16

## Entrée de journal
```
## T-4.7 — GUI de bench — <date> — validée (GATE G-8 posée)
- Fait : Sources/YuE2BenchUI/*.swift
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
