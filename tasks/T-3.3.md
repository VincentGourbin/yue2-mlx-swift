# T-3.3 — Solveur midpoint

Jalon : 3 · Prérequis : T-3.2 · Tier : 1 · ⛔ GATE : non

## Objectif
`solve(steps:)` reproduit le solveur midpoint upstream (état dans le dtype du modèle, `raw` en Double clampé ±20), avec progression et annulation.

## À lire
- `plan/02.4-etape-nar.md` (point 6, solveur midpoint)
- `reference/yue/src/yue2/nar.py` lignes 160-200 (`solve`)

## Fichiers à modifier
- `Sources/YuE2Core/Synthesis/CachedNAR.swift` : `func solve(steps: Int = 32, onProgress: ((Int, Int) -> Void)? = nil, cancel: (() -> Bool)? = nil) throws -> MLXArray [T_c, 64] fp32` : `state = noise.asType(dtype)`, `dt = 1/steps` ; `logit(t) = log(t/(1−t))` en Double, `t = 1 ⇒ +inf ⇒ clamp 20` ; `first = velocity(state, raw)`, `mid = state − first·(dt/2)`, `state = state − velocity(mid, rawMid)·dt` ; `eval(state)` à chaque pas ; profiler : sous-phase `ODE step k` ; résultat `asType(.float32)`, erreur si non fini.
- `Tests/YuE2Tests/NARParityTests.swift` : `solve(steps: 4)` sur `solve4_noise` == `solve4_expected` (1e-4) ; compteur : exactement 8 appels de `velocity` (instrumenter via un callback de test) ; progression `[(1,4),(2,4),(3,4),(4,4)]` ; annulation au 2e pas ⇒ `cancelled`.

## Étapes
1. Implémenter ; tests ; commit `feat(nar): midpoint solver`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh NARParityTests` → `TESTS OK (≥ 10 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°4, 5

## Entrée de journal
```
## T-3.3 — Solveur midpoint — <date> — validée
- Fait : CachedNAR.solve, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
