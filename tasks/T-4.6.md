# T-4.6 — O6/O9 : tuilage des requêtes NAR, CFG batché (conditionnels)

Jalon : 4 · Prérequis : T-4.5 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
O6 seulement si la trace de T-4.0 montre une allocation de scores `[16, L_nar, L_ar+L_nar]` pendant Flow Matching : ajouter `queryChunkSize` (1024) dans `CachedNAR.velocity`. O9 seulement si le mode `off` est utilisé : batcher positif/négatif avec left-padding. Sinon, documenter « non nécessaire » avec la preuve (trace) et passer. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O6
- `reference/qwen38/Sources/Qwen38Core/FlashNext/Qwen4ExpBatchPadding.swift` lignes 1-80 (O9)
- `reference/yue/src/yue2/nar.py` lignes 48-96 (`attention` tuilée, O6)

## Fichiers à modifier
- `Sources/YuE2Core/Synthesis/CachedNAR.swift` (O6) ; `TokenGenerator+CFG.swift` (O9)

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O6 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-parity.sh nar` → `PARITY OK nar …` (O6) ; `RealLMParityTests` mode off (O9)
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°2, 12

## Entrée de journal
```
## T-4.6 — O6/O9 : tuilage des requêtes NAR, CFG batché (conditionnels) — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
