# T-4.3 — O8 : NAR — K/V contigus et précalculs

Jalon : 4 · Prérequis : T-4.2 · Tier : 1 + 2 · ⛔ GATE : non

## Objectif
Préallouer par couche `[1, 8, L_ar + L_nar, 128]` avec le préfixe écrit une fois et la partie NAR écrite en place à chaque velocity (au lieu d'un `concat` par couche) ; `posEmb` et time embedding calculés une fois par appel. Gain mesuré A/B/B/A avec `Scripts/bench-song.sh` ; **retirer** l'optimisation si le gain est < 5 % ; la parité doit rester verte.

## À lire
- `plan/07-jalon4.md` ligne O8
- `Sources/YuE2Core/Synthesis/CachedNAR.swift` (le vôtre)

## Fichiers à modifier
- `Sources/YuE2Core/Synthesis/CachedNAR.swift`

## Étapes
1. Mesure A (référence, `Scripts/bench-song.sh`), implémenter, mesure B, B, A (refroidissement entre chaque).
2. Parité : `Scripts/check-tests.sh` (tier 1 complet) et `Scripts/check-parity.sh` pour le composant touché.
3. Ligne dans `BENCHMARKS.md` (avant/après, commit) et `docs/knowledge/log.md` ; commit `perf: O8 …`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh` → `TESTS OK (…)` (toute la suite tier 1)
3. `Scripts/check-tests.sh NARParityTests` → `TESTS OK (…)` ; `Scripts/check-parity.sh nar` → `PARITY OK nar …`
4. `bash -c 'Scripts/bench-song.sh | tail -1'` → `BENCH …` avec la phase visée plus courte que la référence (ou retrait documenté)

## Critère de fin
Validations vertes ; gain ≥ 5 % consigné, ou optimisation retirée et raison notée.

## Pièges à cocher (plan/09-pieges.md)
n°2, 3, 12

## Entrée de journal
```
## T-4.3 — O8 : NAR — K/V contigus et précalculs — <date> — validée
- Fait : …
- Mesure : avant <s> / après <s> (phase …), dispersion <%>
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
