# T-4.0 — Mesures de référence avant optimisation — ⛔ GATE G-5

Jalon : 4 · Prérequis : T-3.7 · Tier : 2 · ⛔ GATE : **G-5**

## Objectif
Une ligne de référence stable (Release, GPU refroidi) pour juger chaque optimisation, et la question G-5.

## À lire
- `plan/07-jalon4.md`
- `reference/h3/CLAUDE.md` section « Performance work » — `grep -n "Performance work" reference/h3/CLAUDE.md`, lire 25 lignes

## Fichiers à créer
- `Scripts/bench-song.sh` : attend 120 s (refroidissement), lance `yue2 generate --profile` sur `song.json` avec les réglages **courts** (64/100/4 pas) deux fois (A, A), puis écrit dans `.local-runs/bench/<date>.txt` les 4 durées de phase et le pic mémoire ; imprime `BENCH <date> abc=<s> sem=<s> nar=<s> vae=<s> peak=<MB>`.
- `docs/knowledge/benchmarks/bench-reference.tsv` : ligne de référence (commit, date, 4 durées, pic).

## Étapes
1. Script ; deux exécutions ; dispersion intra-variante notée (doit être < 5 %, sinon refroidir plus et recommencer) ; commit `bench: reference measurements`.
2. `tasks/ASK.md` G-5 : tableau des mesures + « quelles optimisations prioriser ? 8-bit voie AR autorisé ? ».

## Validation
1. `bash -c 'Scripts/bench-song.sh | tail -1'` → ligne `BENCH …`
2. `bash -c 'test -s docs/knowledge/benchmarks/bench-reference.tsv && echo REF OK'` → `REF OK`

## Critère de fin
Validations vertes ; G-5 posée ; **terminer**.

## Pièges à cocher (plan/09-pieges.md)
n°15

## Entrée de journal
```
## T-4.0 — Référence de bench — <date> — validée (GATE G-5 posée)
- Fait : Scripts/bench-song.sh, bench-reference.tsv
- Validation : `BENCH …` ×2, dispersion <x> %
- Limites connues : réglages courts (64/100/4) ; la chanson complète est mesurée à part
- Pas d'agent : … · appels d'outils : …
```
