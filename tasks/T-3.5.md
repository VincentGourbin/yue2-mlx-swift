# T-3.5 — Parité real NAR + `yue2 parity nar`

Jalon : 3 · Prérequis : T-3.4 · Tier : 2 · ⛔ GATE : non

## Objectif
Sur les vrais poids (bf16, MPS vs Metal) : velocity à trois t sous 3e-2 d'erreur relative, `solve(4)` sous 5e-2.

## À lire
- `plan/08-tests.md` ligne `nar`
- `reference/yue/src/yue2/nar.py` lignes 100-160 (pour instrumenter les taps côté Python)

## Fichiers à créer / modifier
- `Scripts/reference/real_fixtures.py` sous-commande `nar` : réutilise `prefix_semantic` et `greedy_semantic` (16 premiers tokens, ou les 8 dupliqués) de `lm.safetensors` ; `chunk = Chunk(prefix_semantic + [c + CODEC_OFFSET …] + [MUSIC_END], noise [16,64] seed 42)` ; `engine = CachedNAR(model)` ; `velocity_raw{20,0,-2.3}` (fp32 stockés) + taps `nar_after_pos`, `nar_after_layer_{0,7,14,27}` (copier `velocity` dans le script avec des hooks) ; `solve4_expected = engine.solve(steps=4)`. Écrit `$YUE2_MODELS_DIR/parity/nar.safetensors` (`kind = "real_nar"`).
- `ParityCommand` : `yue2 parity nar` : `PARITY velocity_raw<t>: rel <e>`, `PARITY nar_after_layer_<i>: rel <e>`, `PARITY solve4: rel <e>`, verdict `PARITY OK nar …`.
- `Tests/YuE2Tests/RealNARParityTests.swift` (tier 2).

## Étapes
1. Fixture (≈ 3-6 min MPS) ; CLI + tests ; commit `feat(parity): real NAR parity`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-parity.sh nar` → `PARITY OK nar …`
3. `Scripts/check-tests.sh RealNARParityTests` → `TESTS OK (≥ 2 tests, 0 skippés)`

## Critère de fin
Validations vertes. Si velocity OK mais `solve4` > 5e-2 : noter (dérive bf16 cumulée) et poser la question dans `tasks/ASK.md` avec les valeurs ; ne pas « corriger » en changeant le dtype de l'état.

## Pièges à cocher (plan/09-pieges.md)
n°2, 3, 4, 5, 13

## Entrée de journal
```
## T-3.5 — Parité real NAR — <date> — validée
- Fait : real_fixtures.py nar, parity nar, RealNARParityTests
- Bug réel rencontré : …
- Validation : `PARITY OK nar …`
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
