# T-1.2 — Protocole : constantes, SongRequest, Sampling, préfixes

Jalon : 1 · Prérequis : T-1.0 · Tier : 1 · ⛔ GATE : non

## Objectif
Port ligne à ligne de `protocol.py` : constantes, `SongRequest`, `Sampling`, `GenerationConfig`, `tokenPrefixes`, `negativePrefix`, `chunkRanges`, avec les mêmes validations (erreurs) que la référence.

## À lire
- `plan/02.1-vocabulaire.md`
- `reference/yue/src/yue2/protocol.py` lignes 1-145 (tout)
- `reference/yue/tests/test_protocol.py` lignes 1-120 (cas à reproduire)

## Fichiers à créer
- `Sources/YuE2Core/Protocol/Constants.swift` : `public enum YuE2Token { static let eod = 151643 … }` (toutes les constantes de §2.1), `public enum CoTMode: String, Codable, CaseIterable { case off, melody, full; public var instruction: String }`.
- `Sources/YuE2Core/Protocol/Sampling.swift` : `public struct Sampling: Codable, Equatable` (7 champs, mêmes défauts, `init` qui `throws YuE2Error.invalidRequest` avec les mêmes bornes que `__post_init__`), `public struct GenerationConfig: Codable` (`abc`, `semantic`, `odeSteps = 32`, `odeMethod = "midpoint"`, `context = 24576`, `version = "yue2-native-v1"`) + `static func load(from url: URL)` (décode `yue2_generation_config.json`) + `resolveSampling(overrides:default:)`.
- `Sources/YuE2Core/Protocol/SongRequest.swift` : `public struct SongRequest: Codable, Equatable` (`style`, `lyrics`, `cot = .full`, `seed = 831001`, `abc: String?`, `cfgScale: Double?`, `id = "song"`), `init` validant comme la référence (regex de `id`, bornes de seed et cfg, `abc` interdit avec off), `var guidance: Double`, `func text() -> String`.
- `Sources/YuE2Core/Protocol/Prefixes.swift` : `public protocol TextEncoding { func encode(_ text: String) -> [Int] }` (le tokenizer réel arrive en T-1.5) ; `public enum Prefixes { static func tokenPrefixes(request:tokenizer:abcIDs:) throws -> [Int]; static func negativePrefix(request:tokenizer:abcIDs:) throws -> [Int]; static func chunkRanges(frames:prefixTokens:context:) throws -> [(Int, Int)] }`.
- `Tests/YuE2Tests/ProtocolTests.swift` : suite `ProtocolTests` avec un `FakeTokenizer` (`encode` = un id par caractère, valeur `Int(scalar) % 1000`) ; tests : instruction exacte des 3 modes ; `text()` == `"\(instruction)\n[Tags]\n\(style)\n[Lyrics]\n\(lyrics)\n"` ; préfixes des 3 modes (forme de §2.1) ; négatif ; `chunkRanges(11, 2, 15) == [(0,5),(5,10),(10,11)]` ; erreurs : `cot` invalide, `abc` avec off, `abc` vide, `cfgScale` 21, `id` `..`, `Sampling(topK: 0)`, `Sampling(minTokens: 5, maxTokens: 4)`, abc id ≥ EOD refusé ; `guidance` = 1.0 (full) / 1.01 (off) / valeur fournie.

## Étapes
1. Constants → Sampling → SongRequest → Prefixes → tests. Build après chaque fichier.
2. Commit `feat(protocol): constants, request, sampling, prefixes`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh ProtocolTests` → `TESTS OK (≥ 12 tests, 0 skippés)`

## Critère de fin
Validations vertes ; aucun nombre magique hors de `Constants.swift`.

## Pièges à cocher (plan/09-pieges.md)
n°6 (bornes d'ids), 16

## Entrée de journal
```
## T-1.2 — Protocole — <date> — validée
- Fait : Protocol/{Constants,Sampling,SongRequest,Prefixes}.swift, ProtocolTests.swift
- Bug réel rencontré : …
- Validation : `Scripts/check-build.sh` → `BUILD OK` ; `Scripts/check-tests.sh ProtocolTests` → `TESTS OK (…)`
- Limites connues : le tokenizer réel arrive en T-1.5 (FakeTokenizer ici)
- Pas d'agent : … · appels d'outils : …
```
