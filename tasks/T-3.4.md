# T-3.4 — Synthesizer multi-chunks

Jalon : 3 · Prérequis : T-3.3 · Tier : 1 · ⛔ GATE : non

## Objectif
`synthesize(prefix:codec:seed:)` enchaîne les chunks en série, libère chaque cache, concatène en fp32.

## À lire
- `reference/yue/src/yue2/nar.py` lignes 225-261 (`synthesize`)

## Fichiers à créer
- `Sources/YuE2Core/Synthesis/Synthesizer.swift` : `public struct Synthesizer { init(model:, config: GenerationConfig) ; func synthesize(prefix: [Int], codec: [Int], seed: UInt64, steps: Int? = nil, context: Int? = nil, reuseCache: KVCache? = nil, noise: MLXArray? = nil, onProgress: ((Int, Int) -> Void)?, cancel:) throws -> MLXArray [T, 64] fp32 }` — `YuE2MemoryManager.configure(.nar)` ; `reuseCache` n'est utilisé que si un seul chunk (O2, T-4.2 : ici accepter le paramètre, l'ignorer avec un `YuE2Debug.log`) ; progression globale `chunkIndex·steps + k` sur `steps·chunks` ; `Memory.clearCache()` après chaque chunk.
- `Tests/YuE2Tests/NARParityTests.swift` : tiny, constantes monkeypatchées, `synthesize([2,3], [1]*11, seed 42, steps 2, context 15)` == concat des `CachedNAR(...).solve(steps: 2)` de chaque chunk (bit-à-bit, même bruit injecté) ; progression `(1,6)…(6,6)` ; NaN injecté (velocity remplacée) ⇒ `nonFiniteLatents`.

## Étapes
1. Implémenter ; tests ; commit `feat(nar): serial multi-chunk synthesis`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh NARParityTests` → `TESTS OK (≥ 13 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°5, 8

## Entrée de journal
```
## T-3.4 — Synthesizer — <date> — validée
- Fait : Synthesis/Synthesizer.swift, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : reuseCache ignoré jusqu'à T-4.2
- Pas d'agent : … · appels d'outils : …
```
