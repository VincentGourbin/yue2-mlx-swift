# T-1.9 — Décodage tuilé

Jalon : 1 · Prérequis : T-1.8 · Tier : 1 · ⛔ GATE : non

## Objectif
`decodeTiled(z, coreFrames:, haloFrames:, onProgress:)` borné en mémoire, **identique** au décodage plein (pas de crossfade).

## À lire
- `plan/02.5-vae.md` (paragraphe « Décodage tuilé »)
- `reference/yue/src/yue2/modeling_vae.py` : `grep -n "def decode_tiled" reference/yue/src/yue2/modeling_vae.py`, lire 40 lignes

## Fichiers à modifier
- `Sources/YuE2Core/Models/VAE/YuE2VAE.swift` : `public func decodeTiled(_ z: MLXArray, coreFrames: Int = 1024, haloFrames: Int = 16, onProgress: ((Int, Int) -> Void)? = nil) throws -> MLXArray` — `haloFrames < 16 → YuE2Error.invalidRequest("halo")` ; sortie préallouée `[1, total, 2]` (ou concaténation des crops, puis `eval`) ; pour chaque tuile : `left = max(0, start − halo)`, `right = min(T, end + halo)`, `tile = decode(z[left..<right])`, `cropStart = (start − left)·1920`, `crop = tile[cropStart ..< cropStart + (outEnd − outStart)]` avec `outEnd = min(end·1920, total)` ; `eval(crop)` par tuile puis libération ; `onProgress(i+1, tiles)`.
- `Tests/YuE2Tests/VAEDecoderTests.swift` : `tiled == full` pour (frames, core) ∈ {(1,1),(15,7),(17,8),(33,16)} sur le tiny (rel 3e-5 / abs 3e-6, plus fenêtre ±128 autour de chaque couture) ; `expected_tiled_T17_core8` et `T33_core16` de la fixture ; halo 0 refusé ; progression `[(1,n)…(n,n)]`.

## Étapes
1. Implémenter, tester, commit `feat(vae): exact tiled decoding`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh VAEDecoderTests` → `TESTS OK (≥ 10 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°11

## Entrée de journal
```
## T-1.9 — Décodage tuilé — <date> — validée
- Fait : YuE2VAE.decodeTiled, tests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
