# T-2.8 — Planner / SemanticGenerator + artefacts

Jalon : 2 · Prérequis : T-2.7 · Tier : 1 · ⛔ GATE : non

## Objectif
`plan()` et `generateSemantic()` comme dans `pipeline.py`, avec sauvegarde/rechargement vérifié du plan.

## À lire
- `reference/yue/src/yue2/pipeline.py` lignes 1-75 (`SymbolicPlan.save/load`) et `grep -n "def plan\|def generate_semantic" reference/yue/src/yue2/pipeline.py` puis 25 lignes chacune

## Fichiers à créer
- `Sources/YuE2Core/Pipeline/SymbolicPlan.swift` : `public struct SymbolicPlan: Codable { request, abc: String?, abcIDs: [Int], prefix: [Int], timing: GenerationTiming?, truncated: Bool ; func save(to dir: URL) throws` (écrit `score.abc`, `abc_tokens.npy`, `prefix.npy`, `plan.json`, `plan_manifest.json` avec SHA-256) ; `static func load(from dir: URL) throws` (vérifie manifeste et cohérence, sinon `YuE2Error.invalidRequest`) }`.
- `Sources/YuE2Core/Pipeline/Planner.swift` : `public struct Planner { init(model:, tokenizer:, config: GenerationConfig) ; func plan(request:, abcSampling: Sampling? = nil, onToken:, cancel:) throws -> SymbolicPlan }` (off ⇒ pas de génération ; `abc` fourni ⇒ `encode(abc)` ; sinon génération phase ABC avec `RestrictedHead` ABC, `bounds` ABC, `legacyOff false`, décodage du texte).
- `Sources/YuE2Core/Pipeline/SemanticGenerator.swift` : `public struct SemanticResult { plan, tokens: [Int] (codec, sans offset), timing, truncated }` ; `func generateSemantic(plan:, sampling:, onToken:, cancel:) throws -> SemanticResult` (vérifie `plan.prefix == tokenPrefixes(...)` ; négatif si `guidance != 1` ; `legacyOff = cot == .off` ; conserve le `KVCache` rempli dans le résultat pour O2 : champ `cache: KVCache?`).
- `Tests/YuE2Tests/PlannerTests.swift` (tier 1, FakeTokenizer + tiny) : `plan` avec `abc` fourni ⇒ préfixe conforme à `parity/protocol.json`… (le FakeTokenizer ne donne pas ces ids : tester la **forme** : `prefix.suffix(2) == [abcEnd, musicStart]`, `prefix.first == eod`) ; `save` puis `load` ⇒ égal ; un octet modifié dans `prefix.npy` ⇒ `load` lève.

## Étapes
1. Implémenter ; tests ; commit `feat(pipeline): planner and semantic generator`.

## Validation
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh PlannerTests` → `TESTS OK (≥ 3 tests, 0 skippés)`

## Critère de fin
Validations vertes.

## Pièges à cocher (plan/09-pieges.md)
n°6, 16

## Entrée de journal
```
## T-2.8 — Planner / SemanticGenerator — <date> — validée
- Fait : Pipeline/{SymbolicPlan,Planner,SemanticGenerator}.swift, PlannerTests
- Bug réel rencontré : …
- Validation : …
- Limites connues : …
- Pas d'agent : … · appels d'outils : …
```
