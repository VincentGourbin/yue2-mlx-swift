# T-6.14 — Head restreint quantifié (Q6 : −0,5 Go sur le pic de la phase plan)

Jalon : 6 · Prérequis : T-6.5 `validée` (mesures iPhone) · Tier : 1 · ⛔ GATE : non

## Objectif
Avec `--quant-head`, `RestrictedHead` ne dé-quantifie plus les lignes autorisées par la phase en fp16 : il garde les lignes packées (`weight`/`scales`/`biases` pris avec `MLX.take`) et calcule les logits par `quantizedMatmul`, exactement comme `QuantizedLinear.callAsFunction`. La phase ABC (`0..<eod`, ≈ 151 k lignes) cesse de reconstituer un head fp16 de ≈ 620 Mo — le pic mesuré sur l'iPhone (3 644 Mo en phase plan) doit descendre sous la cible de 3,5 Go.

## À lire (liste fermée, dans cet ordre)
- `plan/13-ios-backend.md` addendum du 2026-09-22 (pic = phase plan) et `tasks/ASK.md` Q6
- `Sources/YuE2Core/Models/LM/RestrictedHead.swift` (60 lignes)
- `.xcodebuild/SourcePackages/checkouts/mlx-swift/Source/MLX/Ops.swift` `grep "public func quantizedMatmul"` (signature : `x, w, scales:, biases:, transpose: true, groupSize:, bits:, mode:`)

## Fichiers à modifier
- `Sources/YuE2Core/Models/LM/RestrictedHead.swift` — deux représentations : lignes denses (head bf16) ou lignes packées + `quantizedMatmul` ; `eval` des trois tableaux à l'init.

## Étapes
1. Mesure avant : `/usr/bin/time -l yue2 plan --quant int4-mixed --quant-head --precision fp16 --abc-max-tokens 128` (profil mobile) → « maximum resident set size ».
2. Implémenter, `Scripts/check-build.sh`.
3. `Scripts/check-tests.sh QuantizationTests` (`quantHeadRestrictedHeadClosesToBF16` : greedy ≥ 7/8 vs bf16) et `Scripts/check-tests.sh RealLMParityTests` (inchangés, head bf16).
4. Mesure après, même commande ; gain attendu ≈ 450-620 Mo.

## Validation (dans l'ordre, arrêt au premier échec)
1. `Scripts/check-build.sh` → `BUILD OK`
2. `Scripts/check-tests.sh QuantizationTests` → `TESTS OK`
3. `Scripts/check-tests.sh RealLMParityTests` → `TESTS OK`
4. Pic après < pic avant − 400 Mo

## Critère de fin
Validations vertes ; entrée `docs/knowledge/log.md` avec les deux pics ; Q6 de `tasks/ASK.md` marquée résolue.

## Pièges à cocher (plan/09-pieges.md)
n°10 (jamais `asType` sur un `QuantizedLinear.weight` packé — `take` sur les tableaux packés, comme aujourd'hui).
